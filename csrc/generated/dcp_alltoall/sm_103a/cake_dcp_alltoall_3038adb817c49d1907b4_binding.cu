/*
 * Copyright (c) 2026, NVIDIA CORPORATION. All rights reserved.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// Generated by the Cake source exporter — do not edit.
// tvm-ffi direct-source launcher for Cake kernel 'kernel_cake_dcp_alltoall_3038adb817c49d1907b4'.
#include <cuda.h>
#include <cuda_bf16.h>
#include <cuda_fp16.h>
#include <cuda_runtime.h>

#include "tvm_ffi_utils.h"

#include <atomic>
#include <cstdint>
#include <cstring>
#include <mutex>
#include <string>
#include <unordered_map>
#include <vector>

extern "C" __global__ void kernel_cake_dcp_alltoall_3038adb817c49d1907b4(__nv_bfloat16* __restrict__ partial_o, float* __restrict__ softmax_stats, __nv_bfloat16* __restrict__ partial_o_out, float* __restrict__ softmax_stats_out, uint8_t* __restrict__ workspace, unsigned long long workspace_stride_in_u64, int cp_rank, int entry_count, int max_channel_count);


namespace cake_host_shim_da7bb2e3cbacd7b5 {

using tvm::ffi::Optional;
using tvm::ffi::TensorView;

class ScopedCudaDevice {
 public:
  explicit ScopedCudaDevice(int device_id) {
    cudaError_t error = cudaGetDevice(&previous_device_);
    TVM_FFI_CHECK(error == cudaSuccess, RuntimeError)
        << "cudaGetDevice failed before host-shim launch: cudaError="
        << static_cast<int>(error);
    if (previous_device_ != device_id) {
      error = cudaSetDevice(device_id);
      TVM_FFI_CHECK(error == cudaSuccess, RuntimeError)
          << "cudaSetDevice failed before host-shim launch for cuda:"
          << device_id << ": cudaError=" << static_cast<int>(error);
      restore_ = true;
    }
  }

  ScopedCudaDevice(const ScopedCudaDevice&) = delete;
  ScopedCudaDevice& operator=(const ScopedCudaDevice&) = delete;

  ~ScopedCudaDevice() noexcept {
    if (restore_) {
      (void)cudaSetDevice(previous_device_);
    }
  }

 private:
  int previous_device_ = -1;
  bool restore_ = false;
};

inline int64_t CakeDeviceMultiprocessorCount(int device_id) {
  constexpr int kMaxCachedCudaDevices = 64;
  TVM_FFI_CHECK(device_id >= 0 && device_id < kMaxCachedCudaDevices, RuntimeError)
      << "physical-SM-count cache does not cover cuda:" << device_id;
  static std::atomic<int> count_by_device[kMaxCachedCudaDevices]{};
  int cached = count_by_device[device_id].load(std::memory_order_acquire);
  if (cached > 0) return cached;

  int count = 0;
  cudaError_t error = cudaDeviceGetAttribute(
      &count, cudaDevAttrMultiProcessorCount, device_id);
  TVM_FFI_CHECK(error == cudaSuccess && count > 0, RuntimeError)
      << "querying multiProcessorCount failed for cuda:" << device_id
      << ": cudaError=" << static_cast<int>(error) << ", count=" << count;
  // Concurrent first launches may repeat the immutable device query, but all
  // publication is atomic and every later hot-path lookup is one acquire load.
  count_by_device[device_id].store(count, std::memory_order_release);
  return count;
}

inline void CheckCudaTensor(const TensorView& t, const char* name) {
  TVM_FFI_CHECK(t.device().device_type == kDLCUDA, ValueError)
      << name << " must be a CUDA tensor, got device_type=" << (int)t.device().device_type;
}

inline void CheckSameCudaDevice(
    const TensorView& t,
    const TensorView& reference,
    const char* name,
    const char* reference_name) {
  TVM_FFI_CHECK(t.device().device_id == reference.device().device_id, ValueError)
      << name << " must be on the same CUDA device as " << reference_name
      << ": got cuda:" << t.device().device_id
      << " versus cuda:" << reference.device().device_id;
}

inline void CheckCurrentCudaDevice(
    const TensorView& reference,
    const char* reference_name) {
  int current_device = -1;
  cudaError_t error = cudaGetDevice(&current_device);
  TVM_FFI_CHECK(error == cudaSuccess, RuntimeError)
      << "cudaGetDevice failed while validating " << reference_name
      << ": cudaError=" << static_cast<int>(error);
  TVM_FFI_CHECK(current_device == reference.device().device_id, ValueError)
      << "current CUDA device must match " << reference_name
      << ": current=cuda:" << current_device
      << ", tensor=cuda:" << reference.device().device_id;
}

inline void CheckContiguous(const TensorView& t, const char* name) {
  TVM_FFI_CHECK(t.IsContiguous(), ValueError) << name << " must be contiguous";
}

inline void CheckDtype(const TensorView& t, const char* name, int code, int bits, int lanes) {
  DLDataType d = t.dtype();
  TVM_FFI_CHECK((int)d.code == code && (int)d.bits == bits && (int)d.lanes == lanes, TypeError)
      << name << " dtype mismatch: expected DLDataType(code=" << code << ", bits=" << bits
      << ", lanes=" << lanes << "), got (code=" << (int)d.code << ", bits=" << (int)d.bits
      << ", lanes=" << (int)d.lanes << ")";
}

// A logical axis.outer(trailing) folds every source dim above the trailing
// dimensions. Shape products are independent of physical strides, so verify
// the leading dimensions form one dense row-major chain instead of inventing
// a "folded stride". The descriptor reads its exact adjacent physical step
// separately through stride[-(trailing + 1)].
inline void CheckDenseLeadingFold(const TensorView& t, int trailing, const char* name) {
  TVM_FFI_CHECK(trailing > 0 && t.ndim() >= trailing, ValueError)
      << name << " cannot fold leading dimensions above " << trailing
      << " trailing dims from ndim=" << t.ndim();
  int outer_last = t.ndim() - trailing - 1;
  if (outer_last <= 0) {
    return;
  }
  int64_t step = t.stride(outer_last);
  TVM_FFI_CHECK(step > 0, ValueError)
      << name << " physical strides must be positive";
  int64_t expected = step;
  for (int axis = outer_last - 1; axis >= 0; --axis) {
    expected *= t.size(axis + 1);
    if (t.size(axis) > 1) {
      TVM_FFI_CHECK(t.stride(axis) == expected, ValueError)
          << name << " leading dims are not physically foldable above " << trailing
          << " trailing dims: stride(" << axis << ")=" << t.stride(axis)
          << ", expected " << expected;
    }
  }
}

void Run(TensorView arg_partial_o, TensorView arg_softmax_stats, TensorView arg_partial_o_out, TensorView arg_softmax_stats_out, TensorView arg_workspace, int64_t arg_workspace_stride_in_u64, int64_t arg_cp_rank, int64_t arg_entry_count, int64_t arg_max_channel_count, int64_t grid_x, int64_t grid_y, int64_t grid_z) {
  DLDevice dev = arg_partial_o.device();
  ScopedCudaDevice device_guard(dev.device_id);
  CheckCudaTensor(arg_partial_o, "partial_o");
  CheckDtype(arg_partial_o, "partial_o", 4, 16, 1);
  CheckContiguous(arg_partial_o, "partial_o");
  CheckCudaTensor(arg_softmax_stats, "softmax_stats");
  CheckDtype(arg_softmax_stats, "softmax_stats", 2, 32, 1);
  CheckContiguous(arg_softmax_stats, "softmax_stats");
  CheckCudaTensor(arg_partial_o_out, "partial_o_out");
  CheckDtype(arg_partial_o_out, "partial_o_out", 4, 16, 1);
  CheckContiguous(arg_partial_o_out, "partial_o_out");
  CheckCudaTensor(arg_softmax_stats_out, "softmax_stats_out");
  CheckDtype(arg_softmax_stats_out, "softmax_stats_out", 2, 32, 1);
  CheckContiguous(arg_softmax_stats_out, "softmax_stats_out");
  CheckCudaTensor(arg_workspace, "workspace");
  CheckDtype(arg_workspace, "workspace", 1, 8, 1);
  CheckContiguous(arg_workspace, "workspace");
  TVM_FFI_CHECK(arg_workspace_stride_in_u64 >= 0LL && arg_workspace_stride_in_u64 <= 9223372036854775807LL, ValueError)
      << "scalar 'workspace_stride_in_u64' value " << arg_workspace_stride_in_u64
      << " is outside u64 range [0, 9223372036854775807]";
  TVM_FFI_CHECK(arg_cp_rank >= -2147483648LL && arg_cp_rank <= 2147483647LL, ValueError)
      << "scalar 'cp_rank' value " << arg_cp_rank
      << " is outside i32 range [-2147483648, 2147483647]";
  TVM_FFI_CHECK(arg_entry_count >= -2147483648LL && arg_entry_count <= 2147483647LL, ValueError)
      << "scalar 'entry_count' value " << arg_entry_count
      << " is outside i32 range [-2147483648, 2147483647]";
  TVM_FFI_CHECK(arg_max_channel_count >= -2147483648LL && arg_max_channel_count <= 2147483647LL, ValueError)
      << "scalar 'max_channel_count' value " << arg_max_channel_count
      << " is outside i32 range [-2147483648, 2147483647]";
  CheckSameCudaDevice(arg_softmax_stats, arg_partial_o, "softmax_stats", "partial_o");
  CheckSameCudaDevice(arg_partial_o_out, arg_partial_o, "partial_o_out", "partial_o");
  CheckSameCudaDevice(arg_softmax_stats_out, arg_partial_o, "softmax_stats_out", "partial_o");
  CheckSameCudaDevice(arg_workspace, arg_partial_o, "workspace", "partial_o");
  TVM_FFI_CHECK(grid_x > 0 && grid_y > 0 && grid_z > 0, ValueError)
      << "launch grid dimensions must be positive, got (" << grid_x << ", " << grid_y
      << ", " << grid_z << ")";

  cudaStream_t stream = (cudaStream_t)TVMFFIEnvGetStream(dev.device_type, dev.device_id);
  __nv_bfloat16* p_partial_o = static_cast<__nv_bfloat16*>(arg_partial_o.data_ptr());
  float* p_softmax_stats = static_cast<float*>(arg_softmax_stats.data_ptr());
  __nv_bfloat16* p_partial_o_out = static_cast<__nv_bfloat16*>(arg_partial_o_out.data_ptr());
  float* p_softmax_stats_out = static_cast<float*>(arg_softmax_stats_out.data_ptr());
  uint8_t* p_workspace = static_cast<uint8_t*>(arg_workspace.data_ptr());
  uint64_t v_workspace_stride_in_u64 = (uint64_t)arg_workspace_stride_in_u64;
  int32_t v_cp_rank = (int32_t)arg_cp_rank;
  int32_t v_entry_count = (int32_t)arg_entry_count;
  int32_t v_max_channel_count = (int32_t)arg_max_channel_count;
  void* kargs[] = {&p_partial_o, &p_softmax_stats, &p_partial_o_out, &p_softmax_stats_out, &p_workspace, &v_workspace_stride_in_u64, &v_cp_rank, &v_entry_count, &v_max_channel_count};


  dim3 grid((uint32_t)grid_x, (uint32_t)grid_y, (uint32_t)grid_z);
  dim3 block(32u, 2u, 1u);

  // Extended direct-source launch for a linked CUDA kernel symbol.
  cudaLaunchAttribute attrs[1]{};
  int n = 0;
  attrs[n].id = cudaLaunchAttributeProgrammaticStreamSerialization;
  attrs[n].val.programmaticStreamSerializationAllowed = 1;
  ++n;
  cudaLaunchConfig_t config{};
  config.gridDim = grid;
  config.blockDim = block;
  config.dynamicSmemBytes = 1088u;
  config.stream = stream;
  config.attrs = attrs;
  config.numAttrs = n;
  cudaError_t launch_status = cudaLaunchKernelExC(
      &config, reinterpret_cast<const void*>(kernel_cake_dcp_alltoall_3038adb817c49d1907b4), kargs);
  TVM_FFI_CHECK(launch_status == cudaSuccess, RuntimeError)
      << "cudaLaunchKernelExC for kernel_cake_dcp_alltoall_3038adb817c49d1907b4 failed: "
      << cudaGetErrorString(launch_status);
}

}  // namespace cake_host_shim_da7bb2e3cbacd7b5

TVM_FFI_DLL_EXPORT_TYPED_FUNC(run_cp2, cake_host_shim_da7bb2e3cbacd7b5::Run);
