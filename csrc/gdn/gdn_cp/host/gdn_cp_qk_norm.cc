// Copyright (c) 2026 by FlashInfer team.
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// clang-format off
// Generated by FlashInfer source package — do not edit.
// tvm-ffi host launcher for GDN CP-prefill kernel 'kernel_flashinfer_blackwell_gdn_cp_prefill_qk_norm_f16_v1'.
#include <cuda.h>
#include <cuda_runtime.h>

#include "tvm_ffi_utils.h"
#include <tvm/ffi/extra/cuda/cubin_launcher.h>

#include <cstdint>
#include <cstring>
#include <limits>
#include <mutex>
#include <string>
#include <unordered_map>
#include <vector>

TVM_FFI_EMBED_CUBIN(flashinfer_blackwell_gdn_cp_prefill_qk_norm_f16_v1_c6c9603ec8);

namespace gdn_cp_host_shim {

using tvm::ffi::TensorView;

inline int64_t TmaCheckedAdd(int64_t lhs, int64_t rhs) {
  TVM_FFI_CHECK(
      !((rhs > 0 && lhs > std::numeric_limits<int64_t>::max() - rhs) ||
        (rhs < 0 && lhs < std::numeric_limits<int64_t>::min() - rhs)),
      ValueError)
      << "TMA expression addition overflows int64";
  return lhs + rhs;
}

inline int64_t TmaCheckedSub(int64_t lhs, int64_t rhs) {
  TVM_FFI_CHECK(
      !((rhs > 0 && lhs < std::numeric_limits<int64_t>::min() + rhs) ||
        (rhs < 0 && lhs > std::numeric_limits<int64_t>::max() + rhs)),
      ValueError)
      << "TMA expression subtraction overflows int64";
  return lhs - rhs;
}

inline int64_t TmaCheckedMul(int64_t lhs, int64_t rhs) {
  bool overflow = false;
  if (lhs > 0) {
    overflow = rhs > 0 ? lhs > std::numeric_limits<int64_t>::max() / rhs
                       : rhs < std::numeric_limits<int64_t>::min() / lhs;
  } else if (lhs < 0) {
    overflow = rhs > 0 ? lhs < std::numeric_limits<int64_t>::min() / rhs
                       : rhs < 0 && lhs < std::numeric_limits<int64_t>::max() / rhs;
  }
  TVM_FFI_CHECK(!overflow, ValueError)
      << "TMA expression multiplication overflows int64";
  return lhs * rhs;
}

inline int64_t TmaFloorDiv(int64_t numerator, int64_t denominator) {
  TVM_FFI_CHECK(denominator != 0, ValueError)
      << "TMA floor division divisor must be non-zero";
  TVM_FFI_CHECK(
      !(numerator == std::numeric_limits<int64_t>::min() && denominator == -1),
      ValueError)
      << "TMA floor division overflows int64";
  int64_t quotient = numerator / denominator;
  int64_t remainder = numerator % denominator;
  if (remainder != 0 && ((remainder < 0) != (denominator < 0))) {
    quotient = TmaCheckedSub(quotient, 1);
  }
  return quotient;
}

inline int64_t TmaFloorMod(int64_t numerator, int64_t denominator) {
  TVM_FFI_CHECK(denominator != 0, ValueError)
      << "TMA floor modulo divisor must be non-zero";
  TVM_FFI_CHECK(
      !(numerator == std::numeric_limits<int64_t>::min() && denominator == -1),
      ValueError)
      << "TMA floor modulo overflows int64";
  int64_t remainder = numerator % denominator;
  if (remainder != 0 && ((remainder < 0) != (denominator < 0))) {
    remainder = TmaCheckedAdd(remainder, denominator);
  }
  return remainder;
}

inline uint64_t TmaGlobalDim(int64_t value) {
  TVM_FFI_CHECK(value > 0 && value <= (INT64_C(1) << 32), ValueError)
      << "TMA global dimension must be in CUDA tensor-map range 1..2^32, got " << value;
  return static_cast<uint64_t>(value);
}

inline uint64_t TmaGlobalStrideBytes(int64_t logical_elements, int64_t elem_bits) {
  TVM_FFI_CHECK(logical_elements > 0, ValueError)
      << "TMA global stride must resolve to positive logical elements, got " << logical_elements;
  int64_t bit_stride = TmaCheckedMul(logical_elements, elem_bits);
  TVM_FFI_CHECK(bit_stride % 8 == 0, ValueError)
      << "TMA global stride is not byte-addressable: " << logical_elements
      << " logical elements at " << elem_bits << " bits";
  int64_t byte_stride = bit_stride / 8;
  TVM_FFI_CHECK(byte_stride % 16 == 0 && byte_stride < (INT64_C(1) << 40), ValueError)
      << "TMA global byte stride must be a multiple of 16 and less than 2^40, got " << byte_stride;
  return static_cast<uint64_t>(byte_stride);
}

inline void CheckCudaTensor(const TensorView& t, const char* name) {
  TVM_FFI_CHECK(t.device().device_type == kDLCUDA, ValueError)
      << name << " must be a CUDA tensor, got device_type=" << (int)t.device().device_type;
}

inline void CheckSameCudaDevice(
    const TensorView& t,
    const TensorView& reference,
    const char* name,
    const char* reference_name) {
  TVM_FFI_CHECK(t.device().device_id == reference.device().device_id, ValueError)
      << name << " must be on the same CUDA device as " << reference_name
      << ": got cuda:" << t.device().device_id
      << " versus cuda:" << reference.device().device_id;
}

inline void CheckContiguous(const TensorView& t, const char* name) {
  TVM_FFI_CHECK(t.IsContiguous(), ValueError) << name << " must be contiguous";
}

inline void CheckDtype(const TensorView& t, const char* name, int code, int bits, int lanes) {
  DLDataType d = t.dtype();
  TVM_FFI_CHECK((int)d.code == code && (int)d.bits == bits && (int)d.lanes == lanes, TypeError)
      << name << " dtype mismatch: expected DLDataType(code=" << code << ", bits=" << bits
      << ", lanes=" << lanes << "), got (code=" << (int)d.code << ", bits=" << (int)d.bits
      << ", lanes=" << (int)d.lanes << ")";
}

// A logical axis.outer(trailing) folds every source dim above the trailing
// dimensions. Shape products are independent of physical strides, so verify
// the leading dimensions form one dense row-major chain instead of inventing
// a "folded stride". The descriptor reads its exact adjacent physical step
// separately through stride[-(trailing + 1)].
inline void CheckDenseLeadingFold(const TensorView& t, int trailing, const char* name) {
  TVM_FFI_CHECK(trailing > 0 && t.ndim() >= trailing, ValueError)
      << name << " cannot fold leading dimensions above " << trailing
      << " trailing dims from ndim=" << t.ndim();
  int outer_last = t.ndim() - trailing - 1;
  if (outer_last <= 0) {
    return;
  }
  int64_t step = t.stride(outer_last);
  TVM_FFI_CHECK(step > 0, ValueError)
      << name << " physical strides must be positive";
  int64_t expected = step;
  for (int axis = outer_last - 1; axis >= 0; --axis) {
    expected = TmaCheckedMul(expected, t.size(axis + 1));
    if (t.size(axis) > 1) {
      TVM_FFI_CHECK(t.stride(axis) == expected, ValueError)
          << name << " leading dims are not physically foldable above " << trailing
          << " trailing dims: stride(" << axis << ")=" << t.stride(axis)
          << ", expected " << expected;
    }
  }
}

void Run(TensorView arg_q, TensorView arg_k, TensorView arg_q_normalized, TensorView arg_k_normalized, int64_t arg_num_q_heads, int64_t arg_num_k_heads, int64_t grid_x, int64_t grid_y, int64_t grid_z) {
  CheckCudaTensor(arg_q, "q");
  CheckDtype(arg_q, "q", 2, 16, 1);
  CheckContiguous(arg_q, "q");
  CheckCudaTensor(arg_k, "k");
  CheckDtype(arg_k, "k", 2, 16, 1);
  CheckContiguous(arg_k, "k");
  CheckCudaTensor(arg_q_normalized, "q_normalized");
  CheckDtype(arg_q_normalized, "q_normalized", 2, 16, 1);
  CheckContiguous(arg_q_normalized, "q_normalized");
  CheckCudaTensor(arg_k_normalized, "k_normalized");
  CheckDtype(arg_k_normalized, "k_normalized", 2, 16, 1);
  CheckContiguous(arg_k_normalized, "k_normalized");
  TVM_FFI_CHECK(arg_num_q_heads >= -2147483648LL && arg_num_q_heads <= 2147483647LL, ValueError)
      << "scalar 'num_q_heads' value " << arg_num_q_heads
      << " is outside i32 range [-2147483648, 2147483647]";
  TVM_FFI_CHECK(arg_num_k_heads >= -2147483648LL && arg_num_k_heads <= 2147483647LL, ValueError)
      << "scalar 'num_k_heads' value " << arg_num_k_heads
      << " is outside i32 range [-2147483648, 2147483647]";
  CheckSameCudaDevice(arg_k, arg_q, "k", "q");
  CheckSameCudaDevice(arg_q_normalized, arg_q, "q_normalized", "q");
  CheckSameCudaDevice(arg_k_normalized, arg_q, "k_normalized", "q");
  TVM_FFI_CHECK(grid_x > 0 && grid_y > 0 && grid_z > 0, ValueError)
      << "launch grid dimensions must be positive, got (" << grid_x << ", " << grid_y
      << ", " << grid_z << ")";

  DLDevice dev = arg_q.device();
  cudaStream_t stream = (cudaStream_t)TVMFFIEnvGetStream(dev.device_type, dev.device_id);
  void* p_q = arg_q.data_ptr();
  void* p_k = arg_k.data_ptr();
  void* p_q_normalized = arg_q_normalized.data_ptr();
  void* p_k_normalized = arg_k_normalized.data_ptr();
  int32_t v_num_q_heads = (int32_t)arg_num_q_heads;
  int32_t v_num_k_heads = (int32_t)arg_num_k_heads;
  void* kargs[] = {&p_q, &p_k, &p_q_normalized, &p_k_normalized, &v_num_q_heads, &v_num_k_heads};

  static auto kernel = TVM_FFI_EMBED_CUBIN_GET_KERNEL(flashinfer_blackwell_gdn_cp_prefill_qk_norm_f16_v1_c6c9603ec8, "kernel_flashinfer_blackwell_gdn_cp_prefill_qk_norm_f16_v1");
  tvm::ffi::dim3 grid((uint32_t)grid_x, (uint32_t)grid_y, (uint32_t)grid_z);
  tvm::ffi::dim3 block(32u, 1u, 1u);

  TVM_FFI_CHECK_CUBIN_LAUNCHER_CUDA_ERROR(kernel.Launch(kargs, grid, block, stream, 0u));
}

}  // namespace gdn_cp_host_shim

TVM_FFI_DLL_EXPORT_TYPED_FUNC(run_qk_norm, gdn_cp_host_shim::Run);
// clang-format on
