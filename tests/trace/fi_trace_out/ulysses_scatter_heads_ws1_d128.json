{
  "name": "ulysses_scatter_heads_ws1_d128",
  "description": "Ulysses context-parallel scatter-heads transform from per-rank sequence shards to per-rank head shards. The reference models world_size=1; multi-rank correctness is exercised by tests/comm/.",
  "op_type": "comm",
  "tags": [
    "fi_api:flashinfer.comm.ulysses.UlyssesCommunicator.scatter_heads",
    "status:experimental",
    "stage:comm"
  ],
  "axes": {
    "batch_size": {
      "type": "var"
    },
    "local_seq_len": {
      "type": "var",
      "description": "Sequence tokens owned by this rank before scatter."
    },
    "global_num_heads": {
      "type": "var",
      "description": "Head count before rank sharding."
    },
    "world_size": {
      "type": "const",
      "value": 1,
      "description": "fi_trace models the single-rank identity case only."
    },
    "head_dim": {
      "type": "const",
      "value": 128
    }
  },
  "constraints": [
    "world_size == 1"
  ],
  "inputs": {
    "x": {
      "shape": [
        "batch_size",
        "local_seq_len",
        "global_num_heads",
        "head_dim"
      ],
      "dtype": "bfloat16"
    }
  },
  "outputs": {
    "output": {
      "shape": [
        "batch_size",
        "local_seq_len",
        "global_num_heads",
        "head_dim"
      ],
      "dtype": "bfloat16",
      "param": "out"
    }
  },
  "reference": "from __future__ import annotations\nimport math\nimport torch\nimport torch.nn.functional as F\n\n@torch.no_grad()\ndef _ulysses_single_rank_reference(\n    x: torch.Tensor, out: torch.Tensor = None, **_unused\n) -> torch.Tensor:\n    \"\"\"Single-rank reference; multi-rank permutation is tested under tests/comm.\"\"\"\n    result = x.clone()\n    if out is not None:\n        out.copy_(result)\n        return out\n    return result\n",
  "check": "def standard_check(\n    reference_outputs: Any,\n    actual_outputs: Any,\n    *,\n    rtol: Optional[float] = None,\n    atol: Optional[float] = None,\n    max_mismatch_pct: float = 0.0,\n    min_cos_sim: Optional[float] = 1.0 - 1e-3,\n) -> bool:\n    \"\"\"Default trace correctness check used when a template does not override it.\"\"\"\n    from flashinfer.trace import default_check\n\n    return default_check(\n        reference_outputs,\n        actual_outputs,\n        rtol=rtol,\n        atol=atol,\n        max_mismatch_pct=max_mismatch_pct,\n        min_cos_sim=min_cos_sim,\n    )\n"
}
