{
  "name": "gemm_fp8_nt_groupwise_n1536_k7168",
  "description": "FP8 group-wise GEMM C = A @ B.T with per-block scales. Backends: ``cutlass`` (SM100/SM120) and ``trtllm`` (SM90+/SM100). Block granularity defaults to (1, 128, 128). Template captures the trtllm canonical layout: ``a_scale=[M, K//bk]``, ``b_scale=[N//bk, K//bk]`` (note: this is the transposed form of the layout described in flashinfer's gemm_base.py docstring; the kernel actually expects the transpose, matching tests/gemm/test_groupwise_scaled_gemm_fp8.py:128-129 which does ``b_scale.t().contiguous()`` for the trtllm path). The cutlass scale layouts differ; their calls won't axis-match this template and will be skipped by the auto-dump.",
  "op_type": "gemm_fp8",
  "tags": [
    "fi_api:flashinfer.gemm.gemm_base.gemm_fp8_nt_groupwise",
    "status:verified",
    "quantization:float8_e4m3fn"
  ],
  "axes": {
    "M": {
      "type": "var"
    },
    "N": {
      "type": "const",
      "value": 1536
    },
    "K": {
      "type": "const",
      "value": 7168
    },
    "K_div_block": {
      "type": "var",
      "description": "K // scale_block_size_k."
    },
    "N_div_block": {
      "type": "var",
      "description": "N // scale_block_size_n."
    }
  },
  "inputs": {
    "a": {
      "shape": [
        "M",
        "K"
      ],
      "dtype": "float8_e4m3fn"
    },
    "b": {
      "shape": [
        "N",
        "K"
      ],
      "dtype": "float8_e4m3fn"
    },
    "a_scale": {
      "shape": [
        "M",
        "K_div_block"
      ],
      "dtype": "float32"
    },
    "b_scale": {
      "shape": [
        "N_div_block",
        "K_div_block"
      ],
      "dtype": "float32"
    }
  },
  "outputs": {
    "out": {
      "shape": [
        "M",
        "N"
      ],
      "dtype": "bfloat16"
    }
  },
  "reference": "@torch.no_grad()\ndef _gemm_fp8_nt_groupwise_reference(\n    a: torch.Tensor,\n    b: torch.Tensor,\n    a_scale: torch.Tensor,\n    b_scale: torch.Tensor,\n    scale_major_mode=None,\n    out=None,\n    out_dtype=None,\n    **_unused,\n) -> torch.Tensor:\n    \"\"\"Reference for the trtllm-backend canonical layout: ``a_scale`` is\n    ``[M, K//bk]`` and ``b_scale`` is ``[N//bk, K//bk]``. Block size is\n    inferred from ``K // a_scale.shape[1]``. Dequantizes per-block then\n    does ``a @ b.T``.\n\n    Note on layout: flashinfer's docstring at ``gemm_base.py:5681-5683``\n    describes b_scale as ``(k // block_size, n // block_size)`` for the\n    trtllm path, but the kernel actually expects the transposed form \u2014\n    this matches ``tests/gemm/test_groupwise_scaled_gemm_fp8.py:128-129``\n    which does ``b_scale.t().contiguous()`` before calling with\n    ``backend=\"trtllm\"``. sglang's\n    ``layers/quantization/fp8_utils.py`` produces the same transposed\n    layout. The cutlass paths use different shapes; their calls won't\n    axis-match this template and will be skipped by the auto-dump.\n    \"\"\"\n    af = a.to(torch.float32)\n    bf = b.to(torch.float32)\n    M, K = af.shape\n    N, _ = bf.shape\n    bk = K // a_scale.shape[1]\n    # b_scale is [N//bk, K//bk] (trtllm canonical) \u2014 broadcast to [N, K]\n    # then transpose for the C = A @ B.T contraction.\n    a_s = a_scale.to(torch.float32).repeat_interleave(bk, dim=1)[:, :K]\n    b_s = (\n        b_scale.to(torch.float32)\n        .repeat_interleave(bk, dim=0)[:N]\n        .repeat_interleave(bk, dim=1)[:, :K]\n    )\n    af = af * a_s\n    bf = bf * b_s  # both [N, K]; element-wise scale\n    res = af @ bf.T\n    od = out_dtype or torch.bfloat16\n    return res.to(od)\n"
}
