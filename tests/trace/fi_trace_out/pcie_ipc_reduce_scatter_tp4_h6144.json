{
  "name": "pcie_ipc_reduce_scatter_tp4_h6144",
  "description": "PCIe IPC SUM reduce-scatter with rank-major shards. This is a per-rank metadata schema; execution requires a collectively initialized workspace.",
  "op_type": "comm",
  "tags": [
    "fi_api:flashinfer.comm.pcie_ipc_collectives.reduce_scatter.PcieIpcReduceScatterWorkspace.reduce_scatter",
    "stage:comm"
  ],
  "axes": {
    "local_rows": {
      "type": "var",
      "description": "Rows in one rank's shard."
    },
    "total_rows": {
      "type": "var",
      "description": "Rows across all rank-major shards."
    },
    "hidden_dim": {
      "type": "const",
      "value": 6144
    },
    "world_size": {
      "type": "const",
      "value": 4
    }
  },
  "constraints": [
    "total_rows == world_size * local_rows"
  ],
  "inputs": {
    "inp": {
      "shape": [
        "total_rows",
        "hidden_dim"
      ],
      "dtype": "bfloat16"
    }
  },
  "outputs": {
    "output": {
      "shape": [
        "local_rows",
        "hidden_dim"
      ],
      "dtype": "bfloat16",
      "param": "out"
    }
  },
  "check": "def standard_check(\n    reference_outputs: Any,\n    actual_outputs: Any,\n    *,\n    rtol: Optional[float] = None,\n    atol: Optional[float] = None,\n    max_mismatch_pct: float = 0.0,\n    min_cos_sim: Optional[float] = 1.0 - 1e-3,\n) -> bool:\n    \"\"\"Default trace correctness check used when a template does not override it.\"\"\"\n    from flashinfer.trace import default_check\n\n    return default_check(\n        reference_outputs,\n        actual_outputs,\n        rtol=rtol,\n        atol=atol,\n        max_mismatch_pct=max_mismatch_pct,\n        min_cos_sim=min_cos_sim,\n    )\n"
}
