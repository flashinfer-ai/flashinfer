[pytest]
# `experimental` keeps `pytest tests/` (stable lanes) from recursing into
# tests/experimental/; an explicit `pytest tests/experimental/` still collects it.
norecursedirs = test_helpers experimental
addopts = --import-mode=importlib
