/*
Copyright (c) 2026 by FlashInfer team.

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
*/

// Generated by the Cake source exporter — do not edit.
// tvm-ffi direct-source launcher for Cake kernel 'kernel_cake_w4a8_megamoe_ep16'.
#include <cuda.h>
#include <cuda_bf16.h>
#include <cuda_fp16.h>
#include <cuda_runtime.h>

#include "tvm_ffi_utils.h"

#include <atomic>
#include <cstdint>
#include <cstring>
#include <mutex>
#include <string>
#include <unordered_map>
#include <vector>

extern "C" __global__ void kernel_cake_w4a8_megamoe_ep16(__nv_bfloat16* __restrict__ caller_x, float* __restrict__ caller_rw, unsigned int* __restrict__ staged_x, uint8_t* __restrict__ staged_sf, long long* __restrict__ staged_ids, float* __restrict__ staged_rw, const __grid_constant__ CUtensorMap W1, const __grid_constant__ CUtensorMap W2, const __grid_constant__ CUtensorMap X1, const __grid_constant__ CUtensorMap X2, const __grid_constant__ CUtensorMap SW1, const __grid_constant__ CUtensorMap SW2, const __grid_constant__ CUtensorMap SX1, const __grid_constant__ CUtensorMap SX2, float* __restrict__ RW, const __grid_constant__ CUtensorMap Q, uint8_t* __restrict__ QData, uint8_t* __restrict__ SF, unsigned long long* __restrict__ output_peers, uint8_t* __restrict__ slots, uint8_t* __restrict__ output, unsigned int* __restrict__ epilogue_grid, unsigned int* __restrict__ l1_full, unsigned int* __restrict__ l1_empty, unsigned int* __restrict__ l2_full, unsigned int* __restrict__ l2_empty, long long* __restrict__ ids, unsigned long long* __restrict__ send, unsigned long long* __restrict__ rank_counts, unsigned int* __restrict__ indices, unsigned long long* __restrict__ src_peers, unsigned long long* __restrict__ recv_peers, unsigned long long* __restrict__ sum_peers, unsigned int* __restrict__ grid_counter, unsigned int* __restrict__ signals, unsigned int* __restrict__ status, unsigned long long* __restrict__ signal_peers, unsigned int rank, unsigned int live_tokens, unsigned long long* __restrict__ token_peers, unsigned long long* __restrict__ sf_peers, unsigned long long* __restrict__ weight_peers, uint8_t* __restrict__ XData, unsigned int* __restrict__ XSFData, unsigned int* __restrict__ metadata, unsigned long long* __restrict__ recv, unsigned int* __restrict__ claims, unsigned int pool_blocks, int active_n, int valid_m, int epoch, int fc1_tiles, int fc2_tiles, int fc1_k, int fc2_k);


namespace cake_host_shim_97b4a0b8ffd7aba5 {

using tvm::ffi::Optional;
using tvm::ffi::TensorView;

class ScopedCudaDevice {
 public:
  explicit ScopedCudaDevice(int device_id) {
    cudaError_t error = cudaGetDevice(&previous_device_);
    TVM_FFI_CHECK(error == cudaSuccess, RuntimeError)
        << "cudaGetDevice failed before host-shim launch: cudaError="
        << static_cast<int>(error);
    if (previous_device_ != device_id) {
      error = cudaSetDevice(device_id);
      TVM_FFI_CHECK(error == cudaSuccess, RuntimeError)
          << "cudaSetDevice failed before host-shim launch for cuda:"
          << device_id << ": cudaError=" << static_cast<int>(error);
      restore_ = true;
    }
  }

  ScopedCudaDevice(const ScopedCudaDevice&) = delete;
  ScopedCudaDevice& operator=(const ScopedCudaDevice&) = delete;

  ~ScopedCudaDevice() noexcept {
    if (restore_) {
      (void)cudaSetDevice(previous_device_);
    }
  }

 private:
  int previous_device_ = -1;
  bool restore_ = false;
};

inline int64_t CakeDeviceMultiprocessorCount(int device_id) {
  constexpr int kMaxCachedCudaDevices = 64;
  TVM_FFI_CHECK(device_id >= 0 && device_id < kMaxCachedCudaDevices, RuntimeError)
      << "physical-SM-count cache does not cover cuda:" << device_id;
  static std::atomic<int> count_by_device[kMaxCachedCudaDevices]{};
  int cached = count_by_device[device_id].load(std::memory_order_acquire);
  if (cached > 0) return cached;

  int count = 0;
  cudaError_t error = cudaDeviceGetAttribute(
      &count, cudaDevAttrMultiProcessorCount, device_id);
  TVM_FFI_CHECK(error == cudaSuccess && count > 0, RuntimeError)
      << "querying multiProcessorCount failed for cuda:" << device_id
      << ": cudaError=" << static_cast<int>(error) << ", count=" << count;
  // Concurrent first launches may repeat the immutable device query, but all
  // publication is atomic and every later hot-path lookup is one acquire load.
  count_by_device[device_id].store(count, std::memory_order_release);
  return count;
}

inline void CheckCudaTensor(const TensorView& t, const char* name) {
  TVM_FFI_CHECK(t.device().device_type == kDLCUDA, ValueError)
      << name << " must be a CUDA tensor, got device_type=" << (int)t.device().device_type;
}

inline void CheckSameCudaDevice(
    const TensorView& t,
    const TensorView& reference,
    const char* name,
    const char* reference_name) {
  TVM_FFI_CHECK(t.device().device_id == reference.device().device_id, ValueError)
      << name << " must be on the same CUDA device as " << reference_name
      << ": got cuda:" << t.device().device_id
      << " versus cuda:" << reference.device().device_id;
}

inline void CheckCurrentCudaDevice(
    const TensorView& reference,
    const char* reference_name) {
  int current_device = -1;
  cudaError_t error = cudaGetDevice(&current_device);
  TVM_FFI_CHECK(error == cudaSuccess, RuntimeError)
      << "cudaGetDevice failed while validating " << reference_name
      << ": cudaError=" << static_cast<int>(error);
  TVM_FFI_CHECK(current_device == reference.device().device_id, ValueError)
      << "current CUDA device must match " << reference_name
      << ": current=cuda:" << current_device
      << ", tensor=cuda:" << reference.device().device_id;
}

inline void CheckContiguous(const TensorView& t, const char* name) {
  TVM_FFI_CHECK(t.IsContiguous(), ValueError) << name << " must be contiguous";
}

inline void CheckDtype(const TensorView& t, const char* name, int code, int bits, int lanes) {
  DLDataType d = t.dtype();
  TVM_FFI_CHECK((int)d.code == code && (int)d.bits == bits && (int)d.lanes == lanes, TypeError)
      << name << " dtype mismatch: expected DLDataType(code=" << code << ", bits=" << bits
      << ", lanes=" << lanes << "), got (code=" << (int)d.code << ", bits=" << (int)d.bits
      << ", lanes=" << (int)d.lanes << ")";
}

// A logical axis.outer(trailing) folds every source dim above the trailing
// dimensions. Shape products are independent of physical strides, so verify
// the leading dimensions form one dense row-major chain instead of inventing
// a "folded stride". The descriptor reads its exact adjacent physical step
// separately through stride[-(trailing + 1)].
inline void CheckDenseLeadingFold(const TensorView& t, int trailing, const char* name) {
  TVM_FFI_CHECK(trailing > 0 && t.ndim() >= trailing, ValueError)
      << name << " cannot fold leading dimensions above " << trailing
      << " trailing dims from ndim=" << t.ndim();
  int outer_last = t.ndim() - trailing - 1;
  if (outer_last <= 0) {
    return;
  }
  int64_t step = t.stride(outer_last);
  TVM_FFI_CHECK(step > 0, ValueError)
      << name << " physical strides must be positive";
  int64_t expected = step;
  for (int axis = outer_last - 1; axis >= 0; --axis) {
    expected *= t.size(axis + 1);
    if (t.size(axis) > 1) {
      TVM_FFI_CHECK(t.stride(axis) == expected, ValueError)
          << name << " leading dims are not physically foldable above " << trailing
          << " trailing dims: stride(" << axis << ")=" << t.stride(axis)
          << ", expected " << expected;
    }
  }
}

// 2D TMA descriptor for buffer 'W1' — compiled from the
// descriptor's std.Expr global_dim/global_strides/checks record.
inline CUtensorMap EncodeTma_W1(const TensorView& t) {
  TVM_FFI_CHECK(t.ndim() >= 2, ValueError)
      << "TMA source 'W1' must have at least 2 dimensions, got ndim=" << t.ndim();
  TVM_FFI_CHECK(t.stride(-1) == 1, ValueError)
      << "TMA source 'W1' must have unit innermost stride, got " << t.stride(-1);
  int64_t d1 = t.size(t.ndim() - 1);
  TVM_FFI_CHECK(d1 > 0, ValueError)
      << "TMA source 'W1' trailing dims must be positive";
  int64_t outer1 = t.numel() / (d1);
  CheckDenseLeadingFold(t, 1, "W1");
  int64_t s2 = t.stride(t.ndim() - 2) * 2;
  TVM_FFI_CHECK(s2 > 0, ValueError)
      << "TMA source 'W1' physical strides must be positive";
  uint64_t global_dim[2] = {(uint64_t)((2 * d1)), (uint64_t)(outer1)};
  TVM_FFI_CHECK(global_dim[0] > 0 && global_dim[1] > 0, ValueError)
      << "TMA descriptor for 'W1' resolved a non-positive global dim";
  TVM_FFI_CHECK(128u <= global_dim[0] && 128u <= global_dim[1], ValueError)
      << "TMA box (128, 128) exceeds resolved global dims for 'W1'";
  int64_t carrier_stride_0 = s2;
  TVM_FFI_CHECK(carrier_stride_0 >= 0, ValueError)
      << "TMA descriptor for 'W1' resolved global stride 1 negative";
  TVM_FFI_CHECK(carrier_stride_0 != 0 || global_dim[1] == 1, ValueError)
      << "TMA descriptor for 'W1' resolved global stride 1 zero while global dimension 1 is not 1";
  TVM_FFI_CHECK((carrier_stride_0 * 4) % 8 == 0, ValueError)
      << "TMA descriptor for 'W1' resolved global stride 1 to a non-whole-byte offset";
  uint64_t global_strides[1] = {
      (uint64_t)((carrier_stride_0 * 4) / 8),
  };
  uint32_t box_dim[2] = {128u, 128u};
  uint32_t elem_strides[2] = {1u, 1u};
  CUtensorMap tm{};
  const void* tensor_base = static_cast<const char*>(t.data_ptr()) + 0u;
  CUresult r = cuTensorMapEncodeTiled(
      &tm, CU_TENSOR_MAP_DATA_TYPE_16U4_ALIGN16B, 2, const_cast<void*>(tensor_base), global_dim, global_strides, box_dim, elem_strides,
      CU_TENSOR_MAP_INTERLEAVE_NONE, CU_TENSOR_MAP_SWIZZLE_128B, CU_TENSOR_MAP_L2_PROMOTION_NONE,
      CU_TENSOR_MAP_FLOAT_OOB_FILL_NONE);
  TVM_FFI_CHECK(r == CUDA_SUCCESS, RuntimeError)
      << "cuTensorMapEncodeTiled (2D, 'W1') failed: CUresult=" << (int)r;
  return tm;
}

// 2D TMA descriptor for buffer 'W2' — compiled from the
// descriptor's std.Expr global_dim/global_strides/checks record.
inline CUtensorMap EncodeTma_W2(const TensorView& t) {
  TVM_FFI_CHECK(t.ndim() >= 2, ValueError)
      << "TMA source 'W2' must have at least 2 dimensions, got ndim=" << t.ndim();
  TVM_FFI_CHECK(t.stride(-1) == 1, ValueError)
      << "TMA source 'W2' must have unit innermost stride, got " << t.stride(-1);
  int64_t d1 = t.size(t.ndim() - 1);
  TVM_FFI_CHECK(d1 > 0, ValueError)
      << "TMA source 'W2' trailing dims must be positive";
  int64_t outer1 = t.numel() / (d1);
  CheckDenseLeadingFold(t, 1, "W2");
  int64_t s2 = t.stride(t.ndim() - 2) * 2;
  TVM_FFI_CHECK(s2 > 0, ValueError)
      << "TMA source 'W2' physical strides must be positive";
  uint64_t global_dim[2] = {(uint64_t)((2 * d1)), (uint64_t)(outer1)};
  TVM_FFI_CHECK(global_dim[0] > 0 && global_dim[1] > 0, ValueError)
      << "TMA descriptor for 'W2' resolved a non-positive global dim";
  TVM_FFI_CHECK(128u <= global_dim[0] && 128u <= global_dim[1], ValueError)
      << "TMA box (128, 128) exceeds resolved global dims for 'W2'";
  int64_t carrier_stride_0 = s2;
  TVM_FFI_CHECK(carrier_stride_0 >= 0, ValueError)
      << "TMA descriptor for 'W2' resolved global stride 1 negative";
  TVM_FFI_CHECK(carrier_stride_0 != 0 || global_dim[1] == 1, ValueError)
      << "TMA descriptor for 'W2' resolved global stride 1 zero while global dimension 1 is not 1";
  TVM_FFI_CHECK((carrier_stride_0 * 4) % 8 == 0, ValueError)
      << "TMA descriptor for 'W2' resolved global stride 1 to a non-whole-byte offset";
  uint64_t global_strides[1] = {
      (uint64_t)((carrier_stride_0 * 4) / 8),
  };
  uint32_t box_dim[2] = {128u, 128u};
  uint32_t elem_strides[2] = {1u, 1u};
  CUtensorMap tm{};
  const void* tensor_base = static_cast<const char*>(t.data_ptr()) + 0u;
  CUresult r = cuTensorMapEncodeTiled(
      &tm, CU_TENSOR_MAP_DATA_TYPE_16U4_ALIGN16B, 2, const_cast<void*>(tensor_base), global_dim, global_strides, box_dim, elem_strides,
      CU_TENSOR_MAP_INTERLEAVE_NONE, CU_TENSOR_MAP_SWIZZLE_128B, CU_TENSOR_MAP_L2_PROMOTION_NONE,
      CU_TENSOR_MAP_FLOAT_OOB_FILL_NONE);
  TVM_FFI_CHECK(r == CUDA_SUCCESS, RuntimeError)
      << "cuTensorMapEncodeTiled (2D, 'W2') failed: CUresult=" << (int)r;
  return tm;
}

// 2D TMA descriptor for buffer 'X1' — compiled from the
// descriptor's std.Expr global_dim/global_strides/checks record.
inline CUtensorMap EncodeTma_X1(const TensorView& t) {
  TVM_FFI_CHECK(t.ndim() >= 2, ValueError)
      << "TMA source 'X1' must have at least 2 dimensions, got ndim=" << t.ndim();
  TVM_FFI_CHECK(t.stride(-1) == 1, ValueError)
      << "TMA source 'X1' must have unit innermost stride, got " << t.stride(-1);
  int64_t d1 = t.size(t.ndim() - 1);
  TVM_FFI_CHECK(d1 > 0, ValueError)
      << "TMA source 'X1' trailing dims must be positive";
  int64_t outer1 = t.numel() / (d1);
  CheckDenseLeadingFold(t, 1, "X1");
  int64_t s2 = t.stride(t.ndim() - 2) * 1;
  TVM_FFI_CHECK(s2 > 0, ValueError)
      << "TMA source 'X1' physical strides must be positive";
  uint64_t global_dim[2] = {(uint64_t)(d1), (uint64_t)(outer1)};
  TVM_FFI_CHECK(global_dim[0] > 0 && global_dim[1] > 0, ValueError)
      << "TMA descriptor for 'X1' resolved a non-positive global dim";
  TVM_FFI_CHECK(128u <= global_dim[0] && 16u <= global_dim[1], ValueError)
      << "TMA box (128, 16) exceeds resolved global dims for 'X1'";
  int64_t carrier_stride_0 = s2;
  TVM_FFI_CHECK(carrier_stride_0 >= 0, ValueError)
      << "TMA descriptor for 'X1' resolved global stride 1 negative";
  TVM_FFI_CHECK(carrier_stride_0 != 0 || global_dim[1] == 1, ValueError)
      << "TMA descriptor for 'X1' resolved global stride 1 zero while global dimension 1 is not 1";
  TVM_FFI_CHECK((carrier_stride_0 * 8) % 8 == 0, ValueError)
      << "TMA descriptor for 'X1' resolved global stride 1 to a non-whole-byte offset";
  uint64_t global_strides[1] = {
      (uint64_t)((carrier_stride_0 * 8) / 8),
  };
  uint32_t box_dim[2] = {128u, 16u};
  uint32_t elem_strides[2] = {1u, 1u};
  CUtensorMap tm{};
  const void* tensor_base = static_cast<const char*>(t.data_ptr()) + 0u;
  CUresult r = cuTensorMapEncodeTiled(
      &tm, CU_TENSOR_MAP_DATA_TYPE_UINT8, 2, const_cast<void*>(tensor_base), global_dim, global_strides, box_dim, elem_strides,
      CU_TENSOR_MAP_INTERLEAVE_NONE, CU_TENSOR_MAP_SWIZZLE_128B, CU_TENSOR_MAP_L2_PROMOTION_NONE,
      CU_TENSOR_MAP_FLOAT_OOB_FILL_NONE);
  TVM_FFI_CHECK(r == CUDA_SUCCESS, RuntimeError)
      << "cuTensorMapEncodeTiled (2D, 'X1') failed: CUresult=" << (int)r;
  return tm;
}

// 2D TMA descriptor for buffer 'X2' — compiled from the
// descriptor's std.Expr global_dim/global_strides/checks record.
inline CUtensorMap EncodeTma_X2(const TensorView& t) {
  TVM_FFI_CHECK(t.ndim() >= 2, ValueError)
      << "TMA source 'X2' must have at least 2 dimensions, got ndim=" << t.ndim();
  TVM_FFI_CHECK(t.stride(-1) == 1, ValueError)
      << "TMA source 'X2' must have unit innermost stride, got " << t.stride(-1);
  int64_t d1 = t.size(t.ndim() - 1);
  TVM_FFI_CHECK(d1 > 0, ValueError)
      << "TMA source 'X2' trailing dims must be positive";
  int64_t outer1 = t.numel() / (d1);
  CheckDenseLeadingFold(t, 1, "X2");
  int64_t s2 = t.stride(t.ndim() - 2) * 1;
  TVM_FFI_CHECK(s2 > 0, ValueError)
      << "TMA source 'X2' physical strides must be positive";
  uint64_t global_dim[2] = {(uint64_t)(d1), (uint64_t)(outer1)};
  TVM_FFI_CHECK(global_dim[0] > 0 && global_dim[1] > 0, ValueError)
      << "TMA descriptor for 'X2' resolved a non-positive global dim";
  TVM_FFI_CHECK(128u <= global_dim[0] && 16u <= global_dim[1], ValueError)
      << "TMA box (128, 16) exceeds resolved global dims for 'X2'";
  int64_t carrier_stride_0 = s2;
  TVM_FFI_CHECK(carrier_stride_0 >= 0, ValueError)
      << "TMA descriptor for 'X2' resolved global stride 1 negative";
  TVM_FFI_CHECK(carrier_stride_0 != 0 || global_dim[1] == 1, ValueError)
      << "TMA descriptor for 'X2' resolved global stride 1 zero while global dimension 1 is not 1";
  TVM_FFI_CHECK((carrier_stride_0 * 8) % 8 == 0, ValueError)
      << "TMA descriptor for 'X2' resolved global stride 1 to a non-whole-byte offset";
  uint64_t global_strides[1] = {
      (uint64_t)((carrier_stride_0 * 8) / 8),
  };
  uint32_t box_dim[2] = {128u, 16u};
  uint32_t elem_strides[2] = {1u, 1u};
  CUtensorMap tm{};
  const void* tensor_base = static_cast<const char*>(t.data_ptr()) + 0u;
  CUresult r = cuTensorMapEncodeTiled(
      &tm, CU_TENSOR_MAP_DATA_TYPE_UINT8, 2, const_cast<void*>(tensor_base), global_dim, global_strides, box_dim, elem_strides,
      CU_TENSOR_MAP_INTERLEAVE_NONE, CU_TENSOR_MAP_SWIZZLE_128B, CU_TENSOR_MAP_L2_PROMOTION_NONE,
      CU_TENSOR_MAP_FLOAT_OOB_FILL_NONE);
  TVM_FFI_CHECK(r == CUDA_SUCCESS, RuntimeError)
      << "cuTensorMapEncodeTiled (2D, 'X2') failed: CUresult=" << (int)r;
  return tm;
}

// 2D TMA descriptor for buffer 'SW1' — compiled from the
// descriptor's std.Expr global_dim/global_strides/checks record.
inline CUtensorMap EncodeTma_SW1(const TensorView& t) {
  TVM_FFI_CHECK(t.ndim() >= 2, ValueError)
      << "TMA source 'SW1' must have at least 2 dimensions, got ndim=" << t.ndim();
  TVM_FFI_CHECK(t.stride(-1) == 1, ValueError)
      << "TMA source 'SW1' must have unit innermost stride, got " << t.stride(-1);
  int64_t d1 = t.size(t.ndim() - 1);
  TVM_FFI_CHECK(d1 > 0, ValueError)
      << "TMA source 'SW1' trailing dims must be positive";
  int64_t outer1 = t.numel() / (d1);
  CheckDenseLeadingFold(t, 1, "SW1");
  int64_t s2 = t.stride(t.ndim() - 2) * 1;
  TVM_FFI_CHECK(s2 > 0, ValueError)
      << "TMA source 'SW1' physical strides must be positive";
  uint64_t global_dim[2] = {(uint64_t)(d1), (uint64_t)(outer1)};
  TVM_FFI_CHECK(global_dim[0] > 0 && global_dim[1] > 0, ValueError)
      << "TMA descriptor for 'SW1' resolved a non-positive global dim";
  TVM_FFI_CHECK(128u <= global_dim[0] && 1u <= global_dim[1], ValueError)
      << "TMA box (128, 1) exceeds resolved global dims for 'SW1'";
  int64_t carrier_stride_0 = s2;
  TVM_FFI_CHECK(carrier_stride_0 >= 0, ValueError)
      << "TMA descriptor for 'SW1' resolved global stride 1 negative";
  TVM_FFI_CHECK(carrier_stride_0 != 0 || global_dim[1] == 1, ValueError)
      << "TMA descriptor for 'SW1' resolved global stride 1 zero while global dimension 1 is not 1";
  TVM_FFI_CHECK((carrier_stride_0 * 32) % 8 == 0, ValueError)
      << "TMA descriptor for 'SW1' resolved global stride 1 to a non-whole-byte offset";
  uint64_t global_strides[1] = {
      (uint64_t)((carrier_stride_0 * 32) / 8),
  };
  uint32_t box_dim[2] = {128u, 1u};
  uint32_t elem_strides[2] = {1u, 1u};
  CUtensorMap tm{};
  const void* tensor_base = static_cast<const char*>(t.data_ptr()) + 0u;
  CUresult r = cuTensorMapEncodeTiled(
      &tm, CU_TENSOR_MAP_DATA_TYPE_UINT32, 2, const_cast<void*>(tensor_base), global_dim, global_strides, box_dim, elem_strides,
      CU_TENSOR_MAP_INTERLEAVE_NONE, CU_TENSOR_MAP_SWIZZLE_NONE, CU_TENSOR_MAP_L2_PROMOTION_NONE,
      CU_TENSOR_MAP_FLOAT_OOB_FILL_NONE);
  TVM_FFI_CHECK(r == CUDA_SUCCESS, RuntimeError)
      << "cuTensorMapEncodeTiled (2D, 'SW1') failed: CUresult=" << (int)r;
  return tm;
}

// 2D TMA descriptor for buffer 'SW2' — compiled from the
// descriptor's std.Expr global_dim/global_strides/checks record.
inline CUtensorMap EncodeTma_SW2(const TensorView& t) {
  TVM_FFI_CHECK(t.ndim() >= 2, ValueError)
      << "TMA source 'SW2' must have at least 2 dimensions, got ndim=" << t.ndim();
  TVM_FFI_CHECK(t.stride(-1) == 1, ValueError)
      << "TMA source 'SW2' must have unit innermost stride, got " << t.stride(-1);
  int64_t d1 = t.size(t.ndim() - 1);
  TVM_FFI_CHECK(d1 > 0, ValueError)
      << "TMA source 'SW2' trailing dims must be positive";
  int64_t outer1 = t.numel() / (d1);
  CheckDenseLeadingFold(t, 1, "SW2");
  int64_t s2 = t.stride(t.ndim() - 2) * 1;
  TVM_FFI_CHECK(s2 > 0, ValueError)
      << "TMA source 'SW2' physical strides must be positive";
  uint64_t global_dim[2] = {(uint64_t)(d1), (uint64_t)(outer1)};
  TVM_FFI_CHECK(global_dim[0] > 0 && global_dim[1] > 0, ValueError)
      << "TMA descriptor for 'SW2' resolved a non-positive global dim";
  TVM_FFI_CHECK(128u <= global_dim[0] && 1u <= global_dim[1], ValueError)
      << "TMA box (128, 1) exceeds resolved global dims for 'SW2'";
  int64_t carrier_stride_0 = s2;
  TVM_FFI_CHECK(carrier_stride_0 >= 0, ValueError)
      << "TMA descriptor for 'SW2' resolved global stride 1 negative";
  TVM_FFI_CHECK(carrier_stride_0 != 0 || global_dim[1] == 1, ValueError)
      << "TMA descriptor for 'SW2' resolved global stride 1 zero while global dimension 1 is not 1";
  TVM_FFI_CHECK((carrier_stride_0 * 32) % 8 == 0, ValueError)
      << "TMA descriptor for 'SW2' resolved global stride 1 to a non-whole-byte offset";
  uint64_t global_strides[1] = {
      (uint64_t)((carrier_stride_0 * 32) / 8),
  };
  uint32_t box_dim[2] = {128u, 1u};
  uint32_t elem_strides[2] = {1u, 1u};
  CUtensorMap tm{};
  const void* tensor_base = static_cast<const char*>(t.data_ptr()) + 0u;
  CUresult r = cuTensorMapEncodeTiled(
      &tm, CU_TENSOR_MAP_DATA_TYPE_UINT32, 2, const_cast<void*>(tensor_base), global_dim, global_strides, box_dim, elem_strides,
      CU_TENSOR_MAP_INTERLEAVE_NONE, CU_TENSOR_MAP_SWIZZLE_NONE, CU_TENSOR_MAP_L2_PROMOTION_NONE,
      CU_TENSOR_MAP_FLOAT_OOB_FILL_NONE);
  TVM_FFI_CHECK(r == CUDA_SUCCESS, RuntimeError)
      << "cuTensorMapEncodeTiled (2D, 'SW2') failed: CUresult=" << (int)r;
  return tm;
}

// 2D TMA descriptor for buffer 'SX1' — compiled from the
// descriptor's std.Expr global_dim/global_strides/checks record.
inline CUtensorMap EncodeTma_SX1(const TensorView& t) {
  TVM_FFI_CHECK(t.ndim() >= 2, ValueError)
      << "TMA source 'SX1' must have at least 2 dimensions, got ndim=" << t.ndim();
  TVM_FFI_CHECK(t.stride(-1) == 1, ValueError)
      << "TMA source 'SX1' must have unit innermost stride, got " << t.stride(-1);
  int64_t d1 = t.size(t.ndim() - 1);
  TVM_FFI_CHECK(d1 > 0, ValueError)
      << "TMA source 'SX1' trailing dims must be positive";
  int64_t outer1 = t.numel() / (d1);
  CheckDenseLeadingFold(t, 1, "SX1");
  int64_t s2 = t.stride(t.ndim() - 2) * 1;
  TVM_FFI_CHECK(s2 > 0, ValueError)
      << "TMA source 'SX1' physical strides must be positive";
  uint64_t global_dim[2] = {(uint64_t)(d1), (uint64_t)(outer1)};
  TVM_FFI_CHECK(global_dim[0] > 0 && global_dim[1] > 0, ValueError)
      << "TMA descriptor for 'SX1' resolved a non-positive global dim";
  TVM_FFI_CHECK(128u <= global_dim[0] && 1u <= global_dim[1], ValueError)
      << "TMA box (128, 1) exceeds resolved global dims for 'SX1'";
  int64_t carrier_stride_0 = s2;
  TVM_FFI_CHECK(carrier_stride_0 >= 0, ValueError)
      << "TMA descriptor for 'SX1' resolved global stride 1 negative";
  TVM_FFI_CHECK(carrier_stride_0 != 0 || global_dim[1] == 1, ValueError)
      << "TMA descriptor for 'SX1' resolved global stride 1 zero while global dimension 1 is not 1";
  TVM_FFI_CHECK((carrier_stride_0 * 32) % 8 == 0, ValueError)
      << "TMA descriptor for 'SX1' resolved global stride 1 to a non-whole-byte offset";
  uint64_t global_strides[1] = {
      (uint64_t)((carrier_stride_0 * 32) / 8),
  };
  uint32_t box_dim[2] = {128u, 1u};
  uint32_t elem_strides[2] = {1u, 1u};
  CUtensorMap tm{};
  const void* tensor_base = static_cast<const char*>(t.data_ptr()) + 0u;
  CUresult r = cuTensorMapEncodeTiled(
      &tm, CU_TENSOR_MAP_DATA_TYPE_UINT32, 2, const_cast<void*>(tensor_base), global_dim, global_strides, box_dim, elem_strides,
      CU_TENSOR_MAP_INTERLEAVE_NONE, CU_TENSOR_MAP_SWIZZLE_NONE, CU_TENSOR_MAP_L2_PROMOTION_NONE,
      CU_TENSOR_MAP_FLOAT_OOB_FILL_NONE);
  TVM_FFI_CHECK(r == CUDA_SUCCESS, RuntimeError)
      << "cuTensorMapEncodeTiled (2D, 'SX1') failed: CUresult=" << (int)r;
  return tm;
}

// 2D TMA descriptor for buffer 'SX2' — compiled from the
// descriptor's std.Expr global_dim/global_strides/checks record.
inline CUtensorMap EncodeTma_SX2(const TensorView& t) {
  TVM_FFI_CHECK(t.ndim() >= 2, ValueError)
      << "TMA source 'SX2' must have at least 2 dimensions, got ndim=" << t.ndim();
  TVM_FFI_CHECK(t.stride(-1) == 1, ValueError)
      << "TMA source 'SX2' must have unit innermost stride, got " << t.stride(-1);
  int64_t d1 = t.size(t.ndim() - 1);
  TVM_FFI_CHECK(d1 > 0, ValueError)
      << "TMA source 'SX2' trailing dims must be positive";
  int64_t outer1 = t.numel() / (d1);
  CheckDenseLeadingFold(t, 1, "SX2");
  int64_t s2 = t.stride(t.ndim() - 2) * 1;
  TVM_FFI_CHECK(s2 > 0, ValueError)
      << "TMA source 'SX2' physical strides must be positive";
  uint64_t global_dim[2] = {(uint64_t)(d1), (uint64_t)(outer1)};
  TVM_FFI_CHECK(global_dim[0] > 0 && global_dim[1] > 0, ValueError)
      << "TMA descriptor for 'SX2' resolved a non-positive global dim";
  TVM_FFI_CHECK(128u <= global_dim[0] && 1u <= global_dim[1], ValueError)
      << "TMA box (128, 1) exceeds resolved global dims for 'SX2'";
  int64_t carrier_stride_0 = s2;
  TVM_FFI_CHECK(carrier_stride_0 >= 0, ValueError)
      << "TMA descriptor for 'SX2' resolved global stride 1 negative";
  TVM_FFI_CHECK(carrier_stride_0 != 0 || global_dim[1] == 1, ValueError)
      << "TMA descriptor for 'SX2' resolved global stride 1 zero while global dimension 1 is not 1";
  TVM_FFI_CHECK((carrier_stride_0 * 32) % 8 == 0, ValueError)
      << "TMA descriptor for 'SX2' resolved global stride 1 to a non-whole-byte offset";
  uint64_t global_strides[1] = {
      (uint64_t)((carrier_stride_0 * 32) / 8),
  };
  uint32_t box_dim[2] = {128u, 1u};
  uint32_t elem_strides[2] = {1u, 1u};
  CUtensorMap tm{};
  const void* tensor_base = static_cast<const char*>(t.data_ptr()) + 0u;
  CUresult r = cuTensorMapEncodeTiled(
      &tm, CU_TENSOR_MAP_DATA_TYPE_UINT32, 2, const_cast<void*>(tensor_base), global_dim, global_strides, box_dim, elem_strides,
      CU_TENSOR_MAP_INTERLEAVE_NONE, CU_TENSOR_MAP_SWIZZLE_NONE, CU_TENSOR_MAP_L2_PROMOTION_NONE,
      CU_TENSOR_MAP_FLOAT_OOB_FILL_NONE);
  TVM_FFI_CHECK(r == CUDA_SUCCESS, RuntimeError)
      << "cuTensorMapEncodeTiled (2D, 'SX2') failed: CUresult=" << (int)r;
  return tm;
}

// 2D TMA descriptor for buffer 'Q' — compiled from the
// descriptor's std.Expr global_dim/global_strides/checks record.
inline CUtensorMap EncodeTma_Q(const TensorView& t) {
  TVM_FFI_CHECK(t.ndim() >= 2, ValueError)
      << "TMA source 'Q' must have at least 2 dimensions, got ndim=" << t.ndim();
  TVM_FFI_CHECK(t.stride(-1) == 1, ValueError)
      << "TMA source 'Q' must have unit innermost stride, got " << t.stride(-1);
  int64_t d1 = t.size(t.ndim() - 1);
  TVM_FFI_CHECK(d1 > 0, ValueError)
      << "TMA source 'Q' trailing dims must be positive";
  int64_t outer1 = t.numel() / (d1);
  CheckDenseLeadingFold(t, 1, "Q");
  int64_t s2 = t.stride(t.ndim() - 2) * 1;
  TVM_FFI_CHECK(s2 > 0, ValueError)
      << "TMA source 'Q' physical strides must be positive";
  uint64_t global_dim[2] = {(uint64_t)(d1), (uint64_t)(outer1)};
  TVM_FFI_CHECK(global_dim[0] > 0 && global_dim[1] > 0, ValueError)
      << "TMA descriptor for 'Q' resolved a non-positive global dim";
  TVM_FFI_CHECK(64u <= global_dim[0] && 16u <= global_dim[1], ValueError)
      << "TMA box (64, 16) exceeds resolved global dims for 'Q'";
  int64_t carrier_stride_0 = s2;
  TVM_FFI_CHECK(carrier_stride_0 >= 0, ValueError)
      << "TMA descriptor for 'Q' resolved global stride 1 negative";
  TVM_FFI_CHECK(carrier_stride_0 != 0 || global_dim[1] == 1, ValueError)
      << "TMA descriptor for 'Q' resolved global stride 1 zero while global dimension 1 is not 1";
  TVM_FFI_CHECK((carrier_stride_0 * 8) % 8 == 0, ValueError)
      << "TMA descriptor for 'Q' resolved global stride 1 to a non-whole-byte offset";
  uint64_t global_strides[1] = {
      (uint64_t)((carrier_stride_0 * 8) / 8),
  };
  uint32_t box_dim[2] = {64u, 16u};
  uint32_t elem_strides[2] = {1u, 1u};
  CUtensorMap tm{};
  const void* tensor_base = static_cast<const char*>(t.data_ptr()) + 0u;
  CUresult r = cuTensorMapEncodeTiled(
      &tm, CU_TENSOR_MAP_DATA_TYPE_UINT8, 2, const_cast<void*>(tensor_base), global_dim, global_strides, box_dim, elem_strides,
      CU_TENSOR_MAP_INTERLEAVE_NONE, CU_TENSOR_MAP_SWIZZLE_64B, CU_TENSOR_MAP_L2_PROMOTION_NONE,
      CU_TENSOR_MAP_FLOAT_OOB_FILL_NONE);
  TVM_FFI_CHECK(r == CUDA_SUCCESS, RuntimeError)
      << "cuTensorMapEncodeTiled (2D, 'Q') failed: CUresult=" << (int)r;
  return tm;
}

void Run(TensorView arg_caller_x, TensorView arg_caller_rw, TensorView arg_staged_x, TensorView arg_staged_sf, TensorView arg_staged_ids, TensorView arg_staged_rw, TensorView arg_W1, TensorView arg_W2, TensorView arg_X1, TensorView arg_X2, TensorView arg_SW1, TensorView arg_SW2, TensorView arg_SX1, TensorView arg_SX2, TensorView arg_RW, TensorView arg_Q, TensorView arg_QData, TensorView arg_SF, TensorView arg_output_peers, TensorView arg_slots, TensorView arg_output, TensorView arg_epilogue_grid, TensorView arg_l1_full, TensorView arg_l1_empty, TensorView arg_l2_full, TensorView arg_l2_empty, TensorView arg_ids, TensorView arg_send, TensorView arg_rank_counts, TensorView arg_indices, TensorView arg_src_peers, TensorView arg_recv_peers, TensorView arg_sum_peers, TensorView arg_grid_counter, TensorView arg_signals, TensorView arg_status, TensorView arg_signal_peers, int64_t arg_rank, int64_t arg_live_tokens, TensorView arg_token_peers, TensorView arg_sf_peers, TensorView arg_weight_peers, TensorView arg_XData, TensorView arg_XSFData, TensorView arg_metadata, TensorView arg_recv, TensorView arg_claims, int64_t arg_pool_blocks, int64_t arg_active_n, int64_t arg_valid_m, int64_t arg_epoch, int64_t arg_fc1_tiles, int64_t arg_fc2_tiles, int64_t arg_fc1_k, int64_t arg_fc2_k, int64_t grid_x, int64_t grid_y, int64_t grid_z) {
  DLDevice dev = arg_caller_x.device();
  ScopedCudaDevice device_guard(dev.device_id);
  CheckCudaTensor(arg_caller_x, "caller_x");
  CheckDtype(arg_caller_x, "caller_x", 4, 16, 1);
  CheckContiguous(arg_caller_x, "caller_x");
  CheckCudaTensor(arg_caller_rw, "caller_rw");
  CheckDtype(arg_caller_rw, "caller_rw", 2, 32, 1);
  CheckContiguous(arg_caller_rw, "caller_rw");
  CheckCudaTensor(arg_staged_x, "staged_x");
  CheckDtype(arg_staged_x, "staged_x", 1, 32, 1);
  CheckContiguous(arg_staged_x, "staged_x");
  CheckCudaTensor(arg_staged_sf, "staged_sf");
  CheckDtype(arg_staged_sf, "staged_sf", 1, 8, 1);
  CheckContiguous(arg_staged_sf, "staged_sf");
  CheckCudaTensor(arg_staged_ids, "staged_ids");
  CheckDtype(arg_staged_ids, "staged_ids", 0, 64, 1);
  CheckContiguous(arg_staged_ids, "staged_ids");
  CheckCudaTensor(arg_staged_rw, "staged_rw");
  CheckDtype(arg_staged_rw, "staged_rw", 2, 32, 1);
  CheckContiguous(arg_staged_rw, "staged_rw");
  CheckCudaTensor(arg_W1, "W1");
  CheckDtype(arg_W1, "W1", 1, 8, 1);
  CheckCudaTensor(arg_W2, "W2");
  CheckDtype(arg_W2, "W2", 1, 8, 1);
  CheckCudaTensor(arg_X1, "X1");
  CheckDtype(arg_X1, "X1", 1, 8, 1);
  CheckCudaTensor(arg_X2, "X2");
  CheckDtype(arg_X2, "X2", 1, 8, 1);
  CheckCudaTensor(arg_SW1, "SW1");
  CheckDtype(arg_SW1, "SW1", 1, 32, 1);
  CheckCudaTensor(arg_SW2, "SW2");
  CheckDtype(arg_SW2, "SW2", 1, 32, 1);
  CheckCudaTensor(arg_SX1, "SX1");
  CheckDtype(arg_SX1, "SX1", 1, 32, 1);
  CheckCudaTensor(arg_SX2, "SX2");
  CheckDtype(arg_SX2, "SX2", 1, 32, 1);
  CheckCudaTensor(arg_RW, "RW");
  CheckDtype(arg_RW, "RW", 2, 32, 1);
  CheckContiguous(arg_RW, "RW");
  CheckCudaTensor(arg_Q, "Q");
  CheckDtype(arg_Q, "Q", 1, 8, 1);
  CheckCudaTensor(arg_QData, "QData");
  CheckDtype(arg_QData, "QData", 1, 8, 1);
  CheckContiguous(arg_QData, "QData");
  CheckCudaTensor(arg_SF, "SF");
  CheckDtype(arg_SF, "SF", 1, 8, 1);
  CheckContiguous(arg_SF, "SF");
  CheckCudaTensor(arg_output_peers, "output_peers");
  CheckDtype(arg_output_peers, "output_peers", 1, 64, 1);
  CheckContiguous(arg_output_peers, "output_peers");
  CheckCudaTensor(arg_slots, "slots");
  CheckDtype(arg_slots, "slots", 1, 8, 1);
  CheckContiguous(arg_slots, "slots");
  CheckCudaTensor(arg_output, "output");
  CheckDtype(arg_output, "output", 1, 8, 1);
  CheckContiguous(arg_output, "output");
  CheckCudaTensor(arg_epilogue_grid, "epilogue_grid");
  CheckDtype(arg_epilogue_grid, "epilogue_grid", 1, 32, 1);
  CheckContiguous(arg_epilogue_grid, "epilogue_grid");
  CheckCudaTensor(arg_l1_full, "l1_full");
  CheckDtype(arg_l1_full, "l1_full", 1, 32, 1);
  CheckContiguous(arg_l1_full, "l1_full");
  CheckCudaTensor(arg_l1_empty, "l1_empty");
  CheckDtype(arg_l1_empty, "l1_empty", 1, 32, 1);
  CheckContiguous(arg_l1_empty, "l1_empty");
  CheckCudaTensor(arg_l2_full, "l2_full");
  CheckDtype(arg_l2_full, "l2_full", 1, 32, 1);
  CheckContiguous(arg_l2_full, "l2_full");
  CheckCudaTensor(arg_l2_empty, "l2_empty");
  CheckDtype(arg_l2_empty, "l2_empty", 1, 32, 1);
  CheckContiguous(arg_l2_empty, "l2_empty");
  CheckCudaTensor(arg_ids, "ids");
  CheckDtype(arg_ids, "ids", 0, 64, 1);
  CheckContiguous(arg_ids, "ids");
  CheckCudaTensor(arg_send, "send");
  CheckDtype(arg_send, "send", 1, 64, 1);
  CheckContiguous(arg_send, "send");
  CheckCudaTensor(arg_rank_counts, "rank_counts");
  CheckDtype(arg_rank_counts, "rank_counts", 1, 64, 1);
  CheckContiguous(arg_rank_counts, "rank_counts");
  CheckCudaTensor(arg_indices, "indices");
  CheckDtype(arg_indices, "indices", 1, 32, 1);
  CheckContiguous(arg_indices, "indices");
  CheckCudaTensor(arg_src_peers, "src_peers");
  CheckDtype(arg_src_peers, "src_peers", 1, 64, 1);
  CheckContiguous(arg_src_peers, "src_peers");
  CheckCudaTensor(arg_recv_peers, "recv_peers");
  CheckDtype(arg_recv_peers, "recv_peers", 1, 64, 1);
  CheckContiguous(arg_recv_peers, "recv_peers");
  CheckCudaTensor(arg_sum_peers, "sum_peers");
  CheckDtype(arg_sum_peers, "sum_peers", 1, 64, 1);
  CheckContiguous(arg_sum_peers, "sum_peers");
  CheckCudaTensor(arg_grid_counter, "grid_counter");
  CheckDtype(arg_grid_counter, "grid_counter", 1, 32, 1);
  CheckContiguous(arg_grid_counter, "grid_counter");
  CheckCudaTensor(arg_signals, "signals");
  CheckDtype(arg_signals, "signals", 1, 32, 1);
  CheckContiguous(arg_signals, "signals");
  CheckCudaTensor(arg_status, "status");
  CheckDtype(arg_status, "status", 1, 32, 1);
  CheckContiguous(arg_status, "status");
  CheckCudaTensor(arg_signal_peers, "signal_peers");
  CheckDtype(arg_signal_peers, "signal_peers", 1, 64, 1);
  CheckContiguous(arg_signal_peers, "signal_peers");
  TVM_FFI_CHECK(arg_rank >= 0LL && arg_rank <= 4294967295LL, ValueError)
      << "scalar 'rank' value " << arg_rank
      << " is outside u32 range [0, 4294967295]";
  TVM_FFI_CHECK(arg_live_tokens >= 0LL && arg_live_tokens <= 4294967295LL, ValueError)
      << "scalar 'live_tokens' value " << arg_live_tokens
      << " is outside u32 range [0, 4294967295]";
  CheckCudaTensor(arg_token_peers, "token_peers");
  CheckDtype(arg_token_peers, "token_peers", 1, 64, 1);
  CheckContiguous(arg_token_peers, "token_peers");
  CheckCudaTensor(arg_sf_peers, "sf_peers");
  CheckDtype(arg_sf_peers, "sf_peers", 1, 64, 1);
  CheckContiguous(arg_sf_peers, "sf_peers");
  CheckCudaTensor(arg_weight_peers, "weight_peers");
  CheckDtype(arg_weight_peers, "weight_peers", 1, 64, 1);
  CheckContiguous(arg_weight_peers, "weight_peers");
  CheckCudaTensor(arg_XData, "XData");
  CheckDtype(arg_XData, "XData", 1, 8, 1);
  CheckContiguous(arg_XData, "XData");
  CheckCudaTensor(arg_XSFData, "XSFData");
  CheckDtype(arg_XSFData, "XSFData", 1, 32, 1);
  CheckContiguous(arg_XSFData, "XSFData");
  CheckCudaTensor(arg_metadata, "metadata");
  CheckDtype(arg_metadata, "metadata", 1, 32, 1);
  CheckContiguous(arg_metadata, "metadata");
  CheckCudaTensor(arg_recv, "recv");
  CheckDtype(arg_recv, "recv", 1, 64, 1);
  CheckContiguous(arg_recv, "recv");
  CheckCudaTensor(arg_claims, "claims");
  CheckDtype(arg_claims, "claims", 1, 32, 1);
  CheckContiguous(arg_claims, "claims");
  TVM_FFI_CHECK(arg_pool_blocks >= 0LL && arg_pool_blocks <= 4294967295LL, ValueError)
      << "scalar 'pool_blocks' value " << arg_pool_blocks
      << " is outside u32 range [0, 4294967295]";
  TVM_FFI_CHECK(arg_active_n >= -2147483648LL && arg_active_n <= 2147483647LL, ValueError)
      << "scalar 'active_n' value " << arg_active_n
      << " is outside i32 range [-2147483648, 2147483647]";
  TVM_FFI_CHECK(arg_valid_m >= -2147483648LL && arg_valid_m <= 2147483647LL, ValueError)
      << "scalar 'valid_m' value " << arg_valid_m
      << " is outside i32 range [-2147483648, 2147483647]";
  TVM_FFI_CHECK(arg_epoch >= -2147483648LL && arg_epoch <= 2147483647LL, ValueError)
      << "scalar 'epoch' value " << arg_epoch
      << " is outside i32 range [-2147483648, 2147483647]";
  TVM_FFI_CHECK(arg_fc1_tiles >= -2147483648LL && arg_fc1_tiles <= 2147483647LL, ValueError)
      << "scalar 'fc1_tiles' value " << arg_fc1_tiles
      << " is outside i32 range [-2147483648, 2147483647]";
  TVM_FFI_CHECK(arg_fc2_tiles >= -2147483648LL && arg_fc2_tiles <= 2147483647LL, ValueError)
      << "scalar 'fc2_tiles' value " << arg_fc2_tiles
      << " is outside i32 range [-2147483648, 2147483647]";
  TVM_FFI_CHECK(arg_fc1_k >= -2147483648LL && arg_fc1_k <= 2147483647LL, ValueError)
      << "scalar 'fc1_k' value " << arg_fc1_k
      << " is outside i32 range [-2147483648, 2147483647]";
  TVM_FFI_CHECK(arg_fc2_k >= -2147483648LL && arg_fc2_k <= 2147483647LL, ValueError)
      << "scalar 'fc2_k' value " << arg_fc2_k
      << " is outside i32 range [-2147483648, 2147483647]";
  CheckSameCudaDevice(arg_caller_rw, arg_caller_x, "caller_rw", "caller_x");
  CheckSameCudaDevice(arg_staged_x, arg_caller_x, "staged_x", "caller_x");
  CheckSameCudaDevice(arg_staged_sf, arg_caller_x, "staged_sf", "caller_x");
  CheckSameCudaDevice(arg_staged_ids, arg_caller_x, "staged_ids", "caller_x");
  CheckSameCudaDevice(arg_staged_rw, arg_caller_x, "staged_rw", "caller_x");
  CheckSameCudaDevice(arg_W1, arg_caller_x, "W1", "caller_x");
  CheckSameCudaDevice(arg_W2, arg_caller_x, "W2", "caller_x");
  CheckSameCudaDevice(arg_X1, arg_caller_x, "X1", "caller_x");
  CheckSameCudaDevice(arg_X2, arg_caller_x, "X2", "caller_x");
  CheckSameCudaDevice(arg_SW1, arg_caller_x, "SW1", "caller_x");
  CheckSameCudaDevice(arg_SW2, arg_caller_x, "SW2", "caller_x");
  CheckSameCudaDevice(arg_SX1, arg_caller_x, "SX1", "caller_x");
  CheckSameCudaDevice(arg_SX2, arg_caller_x, "SX2", "caller_x");
  CheckSameCudaDevice(arg_RW, arg_caller_x, "RW", "caller_x");
  CheckSameCudaDevice(arg_Q, arg_caller_x, "Q", "caller_x");
  CheckSameCudaDevice(arg_QData, arg_caller_x, "QData", "caller_x");
  CheckSameCudaDevice(arg_SF, arg_caller_x, "SF", "caller_x");
  CheckSameCudaDevice(arg_output_peers, arg_caller_x, "output_peers", "caller_x");
  CheckSameCudaDevice(arg_slots, arg_caller_x, "slots", "caller_x");
  CheckSameCudaDevice(arg_output, arg_caller_x, "output", "caller_x");
  CheckSameCudaDevice(arg_epilogue_grid, arg_caller_x, "epilogue_grid", "caller_x");
  CheckSameCudaDevice(arg_l1_full, arg_caller_x, "l1_full", "caller_x");
  CheckSameCudaDevice(arg_l1_empty, arg_caller_x, "l1_empty", "caller_x");
  CheckSameCudaDevice(arg_l2_full, arg_caller_x, "l2_full", "caller_x");
  CheckSameCudaDevice(arg_l2_empty, arg_caller_x, "l2_empty", "caller_x");
  CheckSameCudaDevice(arg_ids, arg_caller_x, "ids", "caller_x");
  CheckSameCudaDevice(arg_send, arg_caller_x, "send", "caller_x");
  CheckSameCudaDevice(arg_rank_counts, arg_caller_x, "rank_counts", "caller_x");
  CheckSameCudaDevice(arg_indices, arg_caller_x, "indices", "caller_x");
  CheckSameCudaDevice(arg_src_peers, arg_caller_x, "src_peers", "caller_x");
  CheckSameCudaDevice(arg_recv_peers, arg_caller_x, "recv_peers", "caller_x");
  CheckSameCudaDevice(arg_sum_peers, arg_caller_x, "sum_peers", "caller_x");
  CheckSameCudaDevice(arg_grid_counter, arg_caller_x, "grid_counter", "caller_x");
  CheckSameCudaDevice(arg_signals, arg_caller_x, "signals", "caller_x");
  CheckSameCudaDevice(arg_status, arg_caller_x, "status", "caller_x");
  CheckSameCudaDevice(arg_signal_peers, arg_caller_x, "signal_peers", "caller_x");
  CheckSameCudaDevice(arg_token_peers, arg_caller_x, "token_peers", "caller_x");
  CheckSameCudaDevice(arg_sf_peers, arg_caller_x, "sf_peers", "caller_x");
  CheckSameCudaDevice(arg_weight_peers, arg_caller_x, "weight_peers", "caller_x");
  CheckSameCudaDevice(arg_XData, arg_caller_x, "XData", "caller_x");
  CheckSameCudaDevice(arg_XSFData, arg_caller_x, "XSFData", "caller_x");
  CheckSameCudaDevice(arg_metadata, arg_caller_x, "metadata", "caller_x");
  CheckSameCudaDevice(arg_recv, arg_caller_x, "recv", "caller_x");
  CheckSameCudaDevice(arg_claims, arg_caller_x, "claims", "caller_x");
  CheckCurrentCudaDevice(arg_caller_x, "caller_x");
  TVM_FFI_CHECK(grid_x > 0 && grid_y > 0 && grid_z > 0, ValueError)
      << "launch grid dimensions must be positive, got (" << grid_x << ", " << grid_y
      << ", " << grid_z << ")";
  TVM_FFI_CHECK(grid_x % 2 == 0 && grid_y % 1 == 0 && grid_z % 1 == 0, ValueError)
      << "launch grid (" << grid_x << ", " << grid_y << ", " << grid_z
      << ") must be divisible by cluster dims (2, 1, 1)";

  cudaStream_t stream = (cudaStream_t)TVMFFIEnvGetStream(dev.device_type, dev.device_id);
  __nv_bfloat16* p_caller_x = static_cast<__nv_bfloat16*>(arg_caller_x.data_ptr());
  float* p_caller_rw = static_cast<float*>(arg_caller_rw.data_ptr());
  unsigned int* p_staged_x = static_cast<unsigned int*>(arg_staged_x.data_ptr());
  uint8_t* p_staged_sf = static_cast<uint8_t*>(arg_staged_sf.data_ptr());
  long long* p_staged_ids = static_cast<long long*>(arg_staged_ids.data_ptr());
  float* p_staged_rw = static_cast<float*>(arg_staged_rw.data_ptr());
  CUtensorMap p_W1 = EncodeTma_W1(arg_W1);
  CUtensorMap p_W2 = EncodeTma_W2(arg_W2);
  CUtensorMap p_X1 = EncodeTma_X1(arg_X1);
  CUtensorMap p_X2 = EncodeTma_X2(arg_X2);
  CUtensorMap p_SW1 = EncodeTma_SW1(arg_SW1);
  CUtensorMap p_SW2 = EncodeTma_SW2(arg_SW2);
  CUtensorMap p_SX1 = EncodeTma_SX1(arg_SX1);
  CUtensorMap p_SX2 = EncodeTma_SX2(arg_SX2);
  float* p_RW = static_cast<float*>(arg_RW.data_ptr());
  CUtensorMap p_Q = EncodeTma_Q(arg_Q);
  uint8_t* p_QData = static_cast<uint8_t*>(arg_QData.data_ptr());
  uint8_t* p_SF = static_cast<uint8_t*>(arg_SF.data_ptr());
  unsigned long long* p_output_peers = static_cast<unsigned long long*>(arg_output_peers.data_ptr());
  uint8_t* p_slots = static_cast<uint8_t*>(arg_slots.data_ptr());
  uint8_t* p_output = static_cast<uint8_t*>(arg_output.data_ptr());
  unsigned int* p_epilogue_grid = static_cast<unsigned int*>(arg_epilogue_grid.data_ptr());
  unsigned int* p_l1_full = static_cast<unsigned int*>(arg_l1_full.data_ptr());
  unsigned int* p_l1_empty = static_cast<unsigned int*>(arg_l1_empty.data_ptr());
  unsigned int* p_l2_full = static_cast<unsigned int*>(arg_l2_full.data_ptr());
  unsigned int* p_l2_empty = static_cast<unsigned int*>(arg_l2_empty.data_ptr());
  long long* p_ids = static_cast<long long*>(arg_ids.data_ptr());
  unsigned long long* p_send = static_cast<unsigned long long*>(arg_send.data_ptr());
  unsigned long long* p_rank_counts = static_cast<unsigned long long*>(arg_rank_counts.data_ptr());
  unsigned int* p_indices = static_cast<unsigned int*>(arg_indices.data_ptr());
  unsigned long long* p_src_peers = static_cast<unsigned long long*>(arg_src_peers.data_ptr());
  unsigned long long* p_recv_peers = static_cast<unsigned long long*>(arg_recv_peers.data_ptr());
  unsigned long long* p_sum_peers = static_cast<unsigned long long*>(arg_sum_peers.data_ptr());
  unsigned int* p_grid_counter = static_cast<unsigned int*>(arg_grid_counter.data_ptr());
  unsigned int* p_signals = static_cast<unsigned int*>(arg_signals.data_ptr());
  unsigned int* p_status = static_cast<unsigned int*>(arg_status.data_ptr());
  unsigned long long* p_signal_peers = static_cast<unsigned long long*>(arg_signal_peers.data_ptr());
  uint32_t v_rank = (uint32_t)arg_rank;
  uint32_t v_live_tokens = (uint32_t)arg_live_tokens;
  unsigned long long* p_token_peers = static_cast<unsigned long long*>(arg_token_peers.data_ptr());
  unsigned long long* p_sf_peers = static_cast<unsigned long long*>(arg_sf_peers.data_ptr());
  unsigned long long* p_weight_peers = static_cast<unsigned long long*>(arg_weight_peers.data_ptr());
  uint8_t* p_XData = static_cast<uint8_t*>(arg_XData.data_ptr());
  unsigned int* p_XSFData = static_cast<unsigned int*>(arg_XSFData.data_ptr());
  unsigned int* p_metadata = static_cast<unsigned int*>(arg_metadata.data_ptr());
  unsigned long long* p_recv = static_cast<unsigned long long*>(arg_recv.data_ptr());
  unsigned int* p_claims = static_cast<unsigned int*>(arg_claims.data_ptr());
  uint32_t v_pool_blocks = (uint32_t)arg_pool_blocks;
  int32_t v_active_n = (int32_t)arg_active_n;
  int32_t v_valid_m = (int32_t)arg_valid_m;
  int32_t v_epoch = (int32_t)arg_epoch;
  int32_t v_fc1_tiles = (int32_t)arg_fc1_tiles;
  int32_t v_fc2_tiles = (int32_t)arg_fc2_tiles;
  int32_t v_fc1_k = (int32_t)arg_fc1_k;
  int32_t v_fc2_k = (int32_t)arg_fc2_k;
  void* kargs[] = {&p_caller_x, &p_caller_rw, &p_staged_x, &p_staged_sf, &p_staged_ids, &p_staged_rw, &p_W1, &p_W2, &p_X1, &p_X2, &p_SW1, &p_SW2, &p_SX1, &p_SX2, &p_RW, &p_Q, &p_QData, &p_SF, &p_output_peers, &p_slots, &p_output, &p_epilogue_grid, &p_l1_full, &p_l1_empty, &p_l2_full, &p_l2_empty, &p_ids, &p_send, &p_rank_counts, &p_indices, &p_src_peers, &p_recv_peers, &p_sum_peers, &p_grid_counter, &p_signals, &p_status, &p_signal_peers, &v_rank, &v_live_tokens, &p_token_peers, &p_sf_peers, &p_weight_peers, &p_XData, &p_XSFData, &p_metadata, &p_recv, &p_claims, &v_pool_blocks, &v_active_n, &v_valid_m, &v_epoch, &v_fc1_tiles, &v_fc2_tiles, &v_fc1_k, &v_fc2_k};

  static std::mutex smem_mu;
  static auto* smem_status_by_device =
      new std::unordered_map<int, cudaError_t>();
  cudaError_t smem_status = cudaSuccess;
  {
    std::lock_guard<std::mutex> lock(smem_mu);
    auto it = smem_status_by_device->find(dev.device_id);
    if (it == smem_status_by_device->end()) {
      int current_device = -1;
      smem_status = cudaGetDevice(&current_device);
      if (smem_status == cudaSuccess && current_device != dev.device_id) {
        smem_status = cudaErrorInvalidDevice;
      }
      if (smem_status == cudaSuccess) {
        smem_status = cudaFuncSetAttribute(
            reinterpret_cast<const void*>(kernel_cake_w4a8_megamoe_ep16),
            cudaFuncAttributeMaxDynamicSharedMemorySize,
            218752);
      }
      smem_status_by_device->emplace(dev.device_id, smem_status);
    } else {
      smem_status = it->second;
    }
  }
  TVM_FFI_CHECK(smem_status == cudaSuccess, RuntimeError)
      << "cudaFuncSetAttribute for kernel_cake_w4a8_megamoe_ep16 failed: "
      << cudaGetErrorString(smem_status);
  dim3 grid((uint32_t)grid_x, (uint32_t)grid_y, (uint32_t)grid_z);
  dim3 block(512u, 1u, 1u);

  cudaLaunchAttribute attrs[2]{};
  int n = 0;
  attrs[n].id = cudaLaunchAttributeClusterDimension;
  attrs[n].val.clusterDim.x = 2u;
  attrs[n].val.clusterDim.y = 1u;
  attrs[n].val.clusterDim.z = 1u;
  ++n;
  attrs[n].id = cudaLaunchAttributeClusterSchedulingPolicyPreference;
  attrs[n].val.clusterSchedulingPolicyPreference = cudaClusterSchedulingPolicySpread;
  ++n;
  cudaLaunchConfig_t config{};
  config.gridDim = grid;
  config.blockDim = block;
  config.dynamicSmemBytes = 218752u;
  config.stream = stream;
  config.attrs = attrs;
  config.numAttrs = n;
  cudaError_t launch_status = cudaLaunchKernelExC(
      &config, reinterpret_cast<const void*>(kernel_cake_w4a8_megamoe_ep16), kargs);
  TVM_FFI_CHECK(launch_status == cudaSuccess, RuntimeError)
      << "cudaLaunchKernelExC for kernel_cake_w4a8_megamoe_ep16 failed: "
      << cudaGetErrorString(launch_status);
}

}  // namespace cake_host_shim_97b4a0b8ffd7aba5

TVM_FFI_DLL_EXPORT_TYPED_FUNC(run, cake_host_shim_97b4a0b8ffd7aba5::Run);
