/*
 * Copyright (c) 2026, NVIDIA CORPORATION. All rights reserved.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// Generated by the Cake source exporter — do not edit.
// tvm-ffi direct-source launcher for Cake kernel 'kernel_cake_kimi_k3_fused_router_421b8a75d822521d2144'.
#include <cuda.h>
#include <cuda_bf16.h>
#include <cuda_fp16.h>
#include <cuda_runtime.h>

#include "tvm_ffi_utils.h"

#include <atomic>
#include <cstdint>
#include <cstring>
#include <mutex>
#include <string>
#include <unordered_map>
#include <vector>

extern "C" __global__ void kernel_cake_kimi_k3_fused_router_421b8a75d822521d2144(float* __restrict__ logits, float* __restrict__ bias, float* __restrict__ topk_weights, int* __restrict__ topk_ids, int* __restrict__ sorted_token_ids, int* __restrict__ expert_ids, int* __restrict__ num_tokens_post_padded, int* __restrict__ expert_counts, int* __restrict__ expert_offsets, int* __restrict__ expert_scatter_offsets, int M);


namespace cake_host_shim_3a3769201b53a75b {

using tvm::ffi::Optional;
using tvm::ffi::TensorView;

class ScopedCudaDevice {
 public:
  explicit ScopedCudaDevice(int device_id) {
    cudaError_t error = cudaGetDevice(&previous_device_);
    TVM_FFI_CHECK(error == cudaSuccess, RuntimeError)
        << "cudaGetDevice failed before host-shim launch: cudaError="
        << static_cast<int>(error);
    if (previous_device_ != device_id) {
      error = cudaSetDevice(device_id);
      TVM_FFI_CHECK(error == cudaSuccess, RuntimeError)
          << "cudaSetDevice failed before host-shim launch for cuda:"
          << device_id << ": cudaError=" << static_cast<int>(error);
      restore_ = true;
    }
  }

  ScopedCudaDevice(const ScopedCudaDevice&) = delete;
  ScopedCudaDevice& operator=(const ScopedCudaDevice&) = delete;

  ~ScopedCudaDevice() noexcept {
    if (restore_) {
      (void)cudaSetDevice(previous_device_);
    }
  }

 private:
  int previous_device_ = -1;
  bool restore_ = false;
};

inline int64_t CakeDeviceMultiprocessorCount(int device_id) {
  constexpr int kMaxCachedCudaDevices = 64;
  TVM_FFI_CHECK(device_id >= 0 && device_id < kMaxCachedCudaDevices, RuntimeError)
      << "physical-SM-count cache does not cover cuda:" << device_id;
  static std::atomic<int> count_by_device[kMaxCachedCudaDevices]{};
  int cached = count_by_device[device_id].load(std::memory_order_acquire);
  if (cached > 0) return cached;

  int count = 0;
  cudaError_t error = cudaDeviceGetAttribute(
      &count, cudaDevAttrMultiProcessorCount, device_id);
  TVM_FFI_CHECK(error == cudaSuccess && count > 0, RuntimeError)
      << "querying multiProcessorCount failed for cuda:" << device_id
      << ": cudaError=" << static_cast<int>(error) << ", count=" << count;
  // Concurrent first launches may repeat the immutable device query, but all
  // publication is atomic and every later hot-path lookup is one acquire load.
  count_by_device[device_id].store(count, std::memory_order_release);
  return count;
}

inline void CheckCudaTensor(const TensorView& t, const char* name) {
  TVM_FFI_CHECK(t.device().device_type == kDLCUDA, ValueError)
      << name << " must be a CUDA tensor, got device_type=" << (int)t.device().device_type;
}

inline void CheckSameCudaDevice(
    const TensorView& t,
    const TensorView& reference,
    const char* name,
    const char* reference_name) {
  TVM_FFI_CHECK(t.device().device_id == reference.device().device_id, ValueError)
      << name << " must be on the same CUDA device as " << reference_name
      << ": got cuda:" << t.device().device_id
      << " versus cuda:" << reference.device().device_id;
}

inline void CheckCurrentCudaDevice(
    const TensorView& reference,
    const char* reference_name) {
  int current_device = -1;
  cudaError_t error = cudaGetDevice(&current_device);
  TVM_FFI_CHECK(error == cudaSuccess, RuntimeError)
      << "cudaGetDevice failed while validating " << reference_name
      << ": cudaError=" << static_cast<int>(error);
  TVM_FFI_CHECK(current_device == reference.device().device_id, ValueError)
      << "current CUDA device must match " << reference_name
      << ": current=cuda:" << current_device
      << ", tensor=cuda:" << reference.device().device_id;
}

inline void CheckContiguous(const TensorView& t, const char* name) {
  TVM_FFI_CHECK(t.IsContiguous(), ValueError) << name << " must be contiguous";
}

inline void CheckDtype(const TensorView& t, const char* name, int code, int bits, int lanes) {
  DLDataType d = t.dtype();
  TVM_FFI_CHECK((int)d.code == code && (int)d.bits == bits && (int)d.lanes == lanes, TypeError)
      << name << " dtype mismatch: expected DLDataType(code=" << code << ", bits=" << bits
      << ", lanes=" << lanes << "), got (code=" << (int)d.code << ", bits=" << (int)d.bits
      << ", lanes=" << (int)d.lanes << ")";
}

// A logical axis.outer(trailing) folds every source dim above the trailing
// dimensions. Shape products are independent of physical strides, so verify
// the leading dimensions form one dense row-major chain instead of inventing
// a "folded stride". The descriptor reads its exact adjacent physical step
// separately through stride[-(trailing + 1)].
inline void CheckDenseLeadingFold(const TensorView& t, int trailing, const char* name) {
  TVM_FFI_CHECK(trailing > 0 && t.ndim() >= trailing, ValueError)
      << name << " cannot fold leading dimensions above " << trailing
      << " trailing dims from ndim=" << t.ndim();
  int outer_last = t.ndim() - trailing - 1;
  if (outer_last <= 0) {
    return;
  }
  int64_t step = t.stride(outer_last);
  TVM_FFI_CHECK(step > 0, ValueError)
      << name << " physical strides must be positive";
  int64_t expected = step;
  for (int axis = outer_last - 1; axis >= 0; --axis) {
    expected *= t.size(axis + 1);
    if (t.size(axis) > 1) {
      TVM_FFI_CHECK(t.stride(axis) == expected, ValueError)
          << name << " leading dims are not physically foldable above " << trailing
          << " trailing dims: stride(" << axis << ")=" << t.stride(axis)
          << ", expected " << expected;
    }
  }
}

void Run(TensorView arg_logits, TensorView arg_bias, TensorView arg_topk_weights, TensorView arg_topk_ids, TensorView arg_sorted_token_ids, TensorView arg_expert_ids, TensorView arg_num_tokens_post_padded, TensorView arg_expert_counts, TensorView arg_expert_offsets, TensorView arg_expert_scatter_offsets, int64_t arg_M, int64_t grid_x, int64_t grid_y, int64_t grid_z) {
  DLDevice dev = arg_logits.device();
  ScopedCudaDevice device_guard(dev.device_id);
  CheckCudaTensor(arg_logits, "logits");
  CheckDtype(arg_logits, "logits", 2, 32, 1);
  CheckContiguous(arg_logits, "logits");
  CheckCudaTensor(arg_bias, "bias");
  CheckDtype(arg_bias, "bias", 2, 32, 1);
  CheckContiguous(arg_bias, "bias");
  CheckCudaTensor(arg_topk_weights, "topk_weights");
  CheckDtype(arg_topk_weights, "topk_weights", 2, 32, 1);
  CheckContiguous(arg_topk_weights, "topk_weights");
  CheckCudaTensor(arg_topk_ids, "topk_ids");
  CheckDtype(arg_topk_ids, "topk_ids", 0, 32, 1);
  CheckContiguous(arg_topk_ids, "topk_ids");
  CheckCudaTensor(arg_sorted_token_ids, "sorted_token_ids");
  CheckDtype(arg_sorted_token_ids, "sorted_token_ids", 0, 32, 1);
  CheckContiguous(arg_sorted_token_ids, "sorted_token_ids");
  CheckCudaTensor(arg_expert_ids, "expert_ids");
  CheckDtype(arg_expert_ids, "expert_ids", 0, 32, 1);
  CheckContiguous(arg_expert_ids, "expert_ids");
  CheckCudaTensor(arg_num_tokens_post_padded, "num_tokens_post_padded");
  CheckDtype(arg_num_tokens_post_padded, "num_tokens_post_padded", 0, 32, 1);
  CheckContiguous(arg_num_tokens_post_padded, "num_tokens_post_padded");
  CheckCudaTensor(arg_expert_counts, "expert_counts");
  CheckDtype(arg_expert_counts, "expert_counts", 0, 32, 1);
  CheckContiguous(arg_expert_counts, "expert_counts");
  CheckCudaTensor(arg_expert_offsets, "expert_offsets");
  CheckDtype(arg_expert_offsets, "expert_offsets", 0, 32, 1);
  CheckContiguous(arg_expert_offsets, "expert_offsets");
  CheckCudaTensor(arg_expert_scatter_offsets, "expert_scatter_offsets");
  CheckDtype(arg_expert_scatter_offsets, "expert_scatter_offsets", 0, 32, 1);
  CheckContiguous(arg_expert_scatter_offsets, "expert_scatter_offsets");
  TVM_FFI_CHECK(arg_M >= -2147483648LL && arg_M <= 2147483647LL, ValueError)
      << "scalar 'M' value " << arg_M
      << " is outside i32 range [-2147483648, 2147483647]";
  CheckSameCudaDevice(arg_bias, arg_logits, "bias", "logits");
  CheckSameCudaDevice(arg_topk_weights, arg_logits, "topk_weights", "logits");
  CheckSameCudaDevice(arg_topk_ids, arg_logits, "topk_ids", "logits");
  CheckSameCudaDevice(arg_sorted_token_ids, arg_logits, "sorted_token_ids", "logits");
  CheckSameCudaDevice(arg_expert_ids, arg_logits, "expert_ids", "logits");
  CheckSameCudaDevice(arg_num_tokens_post_padded, arg_logits, "num_tokens_post_padded", "logits");
  CheckSameCudaDevice(arg_expert_counts, arg_logits, "expert_counts", "logits");
  CheckSameCudaDevice(arg_expert_offsets, arg_logits, "expert_offsets", "logits");
  CheckSameCudaDevice(arg_expert_scatter_offsets, arg_logits, "expert_scatter_offsets", "logits");
  TVM_FFI_CHECK(grid_x > 0 && grid_y > 0 && grid_z > 0, ValueError)
      << "launch grid dimensions must be positive, got (" << grid_x << ", " << grid_y
      << ", " << grid_z << ")";
  TVM_FFI_CHECK(grid_x % 4 == 0 && grid_y % 1 == 0 && grid_z % 1 == 0, ValueError)
      << "launch grid (" << grid_x << ", " << grid_y << ", " << grid_z
      << ") must be divisible by cluster dims (4, 1, 1)";

  cudaStream_t stream = (cudaStream_t)TVMFFIEnvGetStream(dev.device_type, dev.device_id);
  float* p_logits = static_cast<float*>(arg_logits.data_ptr());
  float* p_bias = static_cast<float*>(arg_bias.data_ptr());
  float* p_topk_weights = static_cast<float*>(arg_topk_weights.data_ptr());
  int* p_topk_ids = static_cast<int*>(arg_topk_ids.data_ptr());
  int* p_sorted_token_ids = static_cast<int*>(arg_sorted_token_ids.data_ptr());
  int* p_expert_ids = static_cast<int*>(arg_expert_ids.data_ptr());
  int* p_num_tokens_post_padded = static_cast<int*>(arg_num_tokens_post_padded.data_ptr());
  int* p_expert_counts = static_cast<int*>(arg_expert_counts.data_ptr());
  int* p_expert_offsets = static_cast<int*>(arg_expert_offsets.data_ptr());
  int* p_expert_scatter_offsets = static_cast<int*>(arg_expert_scatter_offsets.data_ptr());
  int32_t v_M = (int32_t)arg_M;
  void* kargs[] = {&p_logits, &p_bias, &p_topk_weights, &p_topk_ids, &p_sorted_token_ids, &p_expert_ids, &p_num_tokens_post_padded, &p_expert_counts, &p_expert_offsets, &p_expert_scatter_offsets, &v_M};


  dim3 grid((uint32_t)grid_x, (uint32_t)grid_y, (uint32_t)grid_z);
  dim3 block(224u, 1u, 1u);

  // Extended direct-source launch for a linked CUDA kernel symbol.
  cudaLaunchAttribute attrs[2]{};
  int n = 0;
  attrs[n].id = cudaLaunchAttributeClusterDimension;
  attrs[n].val.clusterDim.x = 4u;
  attrs[n].val.clusterDim.y = 1u;
  attrs[n].val.clusterDim.z = 1u;
  ++n;
  attrs[n].id = cudaLaunchAttributeClusterSchedulingPolicyPreference;
  attrs[n].val.clusterSchedulingPolicyPreference = cudaClusterSchedulingPolicySpread;
  ++n;
  cudaLaunchConfig_t config{};
  config.gridDim = grid;
  config.blockDim = block;
  config.dynamicSmemBytes = 16896u;
  config.stream = stream;
  config.attrs = attrs;
  config.numAttrs = n;
  cudaError_t launch_status = cudaLaunchKernelExC(
      &config, reinterpret_cast<const void*>(kernel_cake_kimi_k3_fused_router_421b8a75d822521d2144), kargs);
  TVM_FFI_CHECK(launch_status == cudaSuccess, RuntimeError)
      << "cudaLaunchKernelExC for kernel_cake_kimi_k3_fused_router_421b8a75d822521d2144 failed: "
      << cudaGetErrorString(launch_status);
}

}  // namespace cake_host_shim_3a3769201b53a75b

TVM_FFI_DLL_EXPORT_TYPED_FUNC(run, cake_host_shim_3a3769201b53a75b::Run);
